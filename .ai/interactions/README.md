# AI 交互事项目录

本目录用于多人、多 AI 协作时管理事项。

结构：

- `INDEX.md`：事项索引。
- `open/`：当前 open 事项，每个事项一个文件。
- `closed/`：已关闭事项，每个事项一个文件。
- `templates/`：新事项、事件和关闭记录模板。

`AI-INTERACTION-PANEL.md` 只保留 open 事项短索引。过程反馈优先写到任务工作包的 `events/` 目录。
