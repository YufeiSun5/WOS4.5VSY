# Closed 事项

本目录放新规则下已关闭的事项文件。

关闭时必须同步：

- 从 `.ai/interactions/open/` 移到本目录。
- 从 `AI-INTERACTION-PANEL.md` 当前索引移除。
- 更新 `.ai/interactions/INDEX.md`。
- 更新 `MEMORY.md`。
- 如有关联任务工作包，在任务 `events/` 中新增关闭事件。

旧规则的历史归档仍保留在 `.ai/closed-interactions/`。
