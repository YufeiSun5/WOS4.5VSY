# `wos4-blue-client-object-create`

WOS4 蓝色 WebJS 客户端历史记录和运行验证 skill。

主入口是 `SKILL.md`。用于区分蓝色 WebJS 客户端对象与错误的紫色/后台对象，并记录已部署对象的视图验证路径。

当前规范下，`时空对象管理平台`只用于已上线对象的时空信息获取、视图、日志和调试；不得在其中新建内容。旧的对象管理创建路径只作为历史证据保留。
