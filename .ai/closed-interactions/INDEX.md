# 已关闭交互索引

本索引用来按业务语义查找历史事项，不只按日期查找。

## 索引表

| 编号 | 标题 | 状态 | 发起人 AI | 参与人 | 创建时间 | 关闭时间 | 任务工作包 | 归档文件 | 关键词 |
|---|---|---|---|---|---|---|---|---|---|
| - | 暂无已归档事项 | - |  |  |  |  |  |  |  |

## 维护规则

- 每新增一个归档文件，必须在本表新增一行。
- `关键词` 要写业务语义，例如 `蓝色客户端`、`布局`、`回归测试`、`发布部署`，不能只写日期。
- `归档文件` 必须指向 `.ai/closed-interactions/` 下的具体文件。
- 如果事项有关联任务工作包，`任务工作包` 必须填写相对路径。
