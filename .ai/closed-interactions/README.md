# 旧规则已关闭交互归档

本目录保存旧规则下从 `AI-INTERACTION-PANEL.md` 关闭并移出的历史事项。

新规则已迁移到 `.ai/interactions/`：

- 当前事项：`.ai/interactions/open/`
- 新关闭事项：`.ai/interactions/closed/`
- 新索引：`.ai/interactions/INDEX.md`

本目录只读保留，用于查询历史，不再接收新的 close 事项。

## 强制规则

- 查询旧事项时先读本目录的 `AI-ROUTE.md` 和 `INDEX.md`。
- 新事项关闭时不要写入本目录，按 `.ai/docs/ai-interaction-panel-policy.md` 移动到 `.ai/interactions/closed/`。

## 路引文件

- `AI-ROUTE.md`：AI 查询旧历史事项时先读这里。
- `INDEX.md`：旧归档索引，按编号、标题、任务工作包、参与人和关键词定位具体文件。

查询历史事项时不要只按日期猜文件；先查 `INDEX.md`。
