# 元语言函数_基础函数

- 分组：元语言函数说明
- 原始章节名：元语言函数_基础函数
- 内容块数量：84
- 来源：WOS4 帮助手册 static/js/file.js

## 正文

# 元语言_基础函数

# 元语言_基础函数

# 1.1 数学函数

## 1.1 数学函数

# 1.1.1 求绝对值

**double >Abs(double dbNumber)**
求绝对值。
参数名
类型
说明
dbNumber
double
要求绝对值的double类型数字
返回值
说明
绝对值
返回绝对值，表示一个数到原点的距离，比如1的绝对值是1，-1的绝对值也是1。
低级别参数可以隐转为double，也可以求出绝对值。

返回值超出double范围，不做处理，输出什么便是什么
**脚本示例**
// 正确的脚本示例：
double a=1.2;
double b=Abs(a); // b=1.2
// 错误的脚本示例：
string a="abc" ;
double b=Abs(a); // 参数类型错误

### 1.1.1 求绝对值

 double >Abs(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求绝对值。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 要求绝对值的double类型数字 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 绝对值 
 返回绝对值，表示一个数到原点的距离，比如1的绝对值是1，-1的绝对值也是1。
低级别参数可以隐转为double，也可以求出绝对值。 
 
 
 
 返回值超出double范围，不做处理，输出什么便是什么 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=1.2;
double b=Abs(a); // b=1.2
// 错误的脚本示例：
string a="abc" ;
double b=Abs(a); // 参数类型错误
 

# 1.1.2 求平方根

**double >Sqrt(double dbNumber)**
求平方根。
参数名
类型
说明
dbNumber
double
求平方根的double类型数字，非负数
返回值
说明
平方根
返回平方根。低级别参数可以隐转为double，也可以求出平方根。
**脚本示例**
// 正确的脚本示例：
double a=16;
double b=Sqrt(a); // b=4
// 错误的脚本示例：
string a="abc";
double b=Abs(a); // 参数类型错误

### 1.1.2 求平方根

 double >Sqrt(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求平方根。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 求平方根的double类型数字，非负数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 平方根 
 返回平方根。低级别参数可以隐转为double，也可以求出平方根。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=16;
double b=Sqrt(a); // b=4
// 错误的脚本示例：
string a="abc";
double b=Abs(a); // 参数类型错误
 

# 1.1.3 求自然对数

**double >LogE(double dbNumber)**
求自然对数。
参数名
类型
说明
dbNumber
double
真数
返回值
说明
自然对数值
返回自然对数值。低级别参数可以隐转为double，也可以求出对数值。
**脚本示例**
// 正确的脚本示例：
double a=9;
double b=LogE(a); // b=2.197225
// 错误的脚本示例：
string a="abc";
double b=LogE(a); // 参数类型错误

### 1.1.3 求自然对数

 double >LogE(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求自然对数。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 真数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 自然对数值 
 返回自然对数值。低级别参数可以隐转为double，也可以求出对数值。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=9;
double b=LogE(a); // b=2.197225
// 错误的脚本示例：
string a="abc";
double b=LogE(a); // 参数类型错误
 

# 1.1.4 求以10为底的对数

**double >Log(double dbNumber)**
求以10为底的对数。
参数名
类型
说明
dbNumber
double
真数
返回值
说明
以10为底的对数
返回以10为底的对数。低级别参数可以隐转为double，可以求出10为底的对数
**脚本示例**
// 正确的脚本示例：
double a=100; 
double b=Log(a); // b=2
// 错误的脚本示例：
string a="abc";
double b=Log(a); // 参数类型错误

### 1.1.4 求以10为底的对数

 double >Log(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求以10为底的对数。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 真数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 以10为底的对数 
 返回以10为底的对数。低级别参数可以隐转为double，可以求出10为底的对数 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=100; 
double b=Log(a); // b=2
// 错误的脚本示例：
string a="abc";
double b=Log(a); // 参数类型错误
 

# 1.1.5 求自然指数

**double >Exp(double dbNumber)**
求自然指数。
参数名
类型
说明
dbNumber
double
e为底的次方数
返回值
说明
自然指数
返回以e为底的指数值。低级别参数可以隐转为double，也可以求出值。
**脚本示例**
// 正确的脚本示例：
double a=1;
double b=Exp(a); // b=2.718282
// 错误的脚本示例：
string a="abc";
double b=Exp(a); // 参数类型错误

### 1.1.5 求自然指数

 double >Exp(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求自然指数。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 e为底的次方数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 自然指数 
 返回以e为底的指数值。低级别参数可以隐转为double，也可以求出值。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=1;
double b=Exp(a); // b=2.718282
// 错误的脚本示例：
string a="abc";
double b=Exp(a); // 参数类型错误
 

# 1.1.6 求正弦

**double >Sin(double dbNumber)**
求正弦。
参数名
类型
说明
dbNumber
double
参数默认按照弧度处理，用户要输出角度，需要自行转换。
返回值
说明
正弦值
返回正弦值。低级别参数可以隐转为double，也可以求出值。
**脚本示例**
// 正确的脚本示例：
double a=0;
double b=Sin(a); // b=0
// 错误的脚本示例：
string a="abc";
double b=Sin(a); // 参数类型错误

### 1.1.6 求正弦

 double >Sin(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求正弦。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 参数默认按照弧度处理，用户要输出角度，需要自行转换。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 正弦值 
 返回正弦值。低级别参数可以隐转为double，也可以求出值。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=0;
double b=Sin(a); // b=0
// 错误的脚本示例：
string a="abc";
double b=Sin(a); // 参数类型错误
 

# 1.1.7 求余弦

**double >Cos(double dbNumber)**
求余弦。
参数名
类型
说明
dbNumber
double
参数默认按照弧度处理，用户要输出角度，需要自行转换。
返回值
说明
余弦值
返回余弦值。低级别参数可以隐转为double，也可以求出值。
**脚本示例**
// 正确的脚本示例：
double a=0;
double b=Cos(a); // b=1
// 错误的脚本示例：
string a="abc";
double b=Cos(a); // 参数类型错误

### 1.1.7 求余弦

 double >Cos(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求余弦。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 参数默认按照弧度处理，用户要输出角度，需要自行转换。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 余弦值 
 返回余弦值。低级别参数可以隐转为double，也可以求出值。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=0;
double b=Cos(a); // b=1
// 错误的脚本示例：
string a="abc";
double b=Cos(a); // 参数类型错误
 

# 1.1.8 求正切

**double >Tan(double dbNumber)**
求正切。
参数名
类型
说明
dbNumber
double
参数默认按照弧度处理，用户要输出角度，需要自行转换。不等于(kπ+π/2），k是整数，1度=π/180。注：角度转弧度：1度=π/180≈0.01745弧度，弧度转角度：1弧度=180/π≈57.3度
返回值
说明
正切值
返回正切值。低级别参数可以隐转为double，也可以求出值。
返回错误信息，不能等于(kπ+π/2）
参数等于kπ+π/2，k是整数。
**脚本示例**
// 正确的脚本示例：
double a=45;
double b=Tan(a); // b=1.619775
// 错误的脚本示例：
string a="abc";
double b=Tan(a); // 参数类型错误

### 1.1.8 求正切

 double >Tan(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求正切。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 参数默认按照弧度处理，用户要输出角度，需要自行转换。不等于(kπ+π/2），k是整数，1度=π/180。注：角度转弧度：1度=π/180≈0.01745弧度，弧度转角度：1弧度=180/π≈57.3度 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 正切值 
 返回正切值。低级别参数可以隐转为double，也可以求出值。 
 
 
 返回错误信息，不能等于(kπ+π/2） 
 参数等于kπ+π/2，k是整数。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=45;
double b=Tan(a); // b=1.619775
// 错误的脚本示例：
string a="abc";
double b=Tan(a); // 参数类型错误
 

# 1.1.9 求反正弦

**double >ArcSin(double dbNumber)**
求反正弦。
参数名
类型
说明
dbNumber
double
参数范围[-1,1]。
返回值
说明
反正弦值
返回反正弦值，弧度。低级别参数可以隐转为double，也可以求出值。
错误的值/信息（Nan）
参数超出[-1,1]的范围
**脚本示例**
// 正确的脚本示例：
double a=0.5;
double b=ArcSin(a); // b=0.523599
// 错误的脚本示例：
string a="abc";
double b=ArcSin(a); // 参数类型错误

### 1.1.9 求反正弦

 double >ArcSin(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求反正弦。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 参数范围[-1,1]。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 反正弦值 
 返回反正弦值，弧度。低级别参数可以隐转为double，也可以求出值。 
 
 
 错误的值/信息（Nan） 
 参数超出[-1,1]的范围 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=0.5;
double b=ArcSin(a); // b=0.523599
// 错误的脚本示例：
string a="abc";
double b=ArcSin(a); // 参数类型错误
 

# 1.1.10 求反余弦

**double >ArcCos(double dbNumber)**
求反余弦。
参数名
类型
说明
dbNumber
double
参数范围[-1,1]
返回值
说明
反余弦值
返回反余弦值，弧度。低级别参数可以隐转为double，也可以求出值。
错误的值/信息（Nan）
参数超出[-1,1]的范围
**脚本示例**
// 正确的脚本示例：
double a=0.5;
double b=ArcCos(a); // b=1.047198
// 错误的脚本示例：
string a="abc";
double b=ArcCos(a); // 参数类型错误

### 1.1.10 求反余弦

 double >ArcCos(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求反余弦。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 参数范围[-1,1] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 反余弦值 
 返回反余弦值，弧度。低级别参数可以隐转为double，也可以求出值。 
 
 
 错误的值/信息（Nan） 
 参数超出[-1,1]的范围 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=0.5;
double b=ArcCos(a); // b=1.047198
// 错误的脚本示例：
string a="abc";
double b=ArcCos(a); // 参数类型错误
 

# 1.1.11 求反正切

**double >ArcTan(double dbNumber)**
求反正切。
参数名
类型
说明
dbNumber
double
double参数范围内
返回值
说明
反正切值
返回反正切值，弧度。低级别参数可以隐转为double，也可以求出值。
**脚本示例**
// 正确的脚本示例：
double a=0.5;
double b=ArcTan(a); // b=0.463648
// 错误的脚本示例：
string a="abc";
double b=ArcTan(a); // 参数类型错误

### 1.1.11 求反正切

 double >ArcTan(double dbNumber) 

 
 
 
 功能描述 
 
 
 
 求反正切。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dbNumber 
 double 
 double参数范围内 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 反正切值 
 返回反正切值，弧度。低级别参数可以隐转为double，也可以求出值。 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=0.5;
double b=ArcTan(a); // b=0.463648
// 错误的脚本示例：
string a="abc";
double b=ArcTan(a); // 参数类型错误
 

# 1.1.12 求次方

**double >Pow(double x,double y)**
求次方，返回x的y次方值。
参数名
类型
说明
x
double
底数。
y
double
指数。
返回值
说明
x的y次方值
返回x的y次方值值。低级别参数可以隐转为double，也可以求出值。负数（负整数、负小数）的（正负）小数次方无意义。(-nan）

负数的小数次方。不做处理，输出什么便是什么
**脚本示例**
// 正确的脚本示例：
double a=10;
int8 b=2;
double c=Pow(a,b); // b=100
// 错误的脚本示例：
string a="abc";
int8 b=2;
double c=Pow(a); // 第一个参数类型错误

### 1.1.12 求次方

 double >Pow(double x,double y) 

 
 
 
 功能描述 
 
 
 
 求次方，返回x的y次方值。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 x 
 double 
 底数。 
 
 
 y 
 double 
 指数。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 x的y次方值 
 返回x的y次方值值。低级别参数可以隐转为double，也可以求出值。负数（负整数、负小数）的（正负）小数次方无意义。(-nan） 
 
 
 
 负数的小数次方。不做处理，输出什么便是什么 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=10;
int8 b=2;
double c=Pow(a,b); // b=100
// 错误的脚本示例：
string a="abc";
int8 b=2;
double c=Pow(a); // 第一个参数类型错误
 

# 1.1.13 取余

**double >Mod(double number,double divisor)**
取余，返回两数相除的余数。
参数名
类型
说明
number
double
double类型数字。被除数
divisor
double
double类型数字。除数，不能为0。
返回值
说明
余数
返回两数相除的余数。结果的正负号与被除数相同。
double最大值（1.7976931348623158e+308）
错误（除数不能为0）
**脚本示例**
// 正确的脚本示例：
double a=5.0;
double b=4.0;
double res = Mod(a,b); // res=1.0
// 错误的脚本示例：
string a="abc";
double b=4.0;
double res = Mod(a,b); // 参数类型错误

### 1.1.13 取余

 double >Mod(double number,double divisor) 

 
 
 
 功能描述 
 
 
 
 取余，返回两数相除的余数。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 number 
 double 
 double类型数字。被除数 
 
 
 divisor 
 double 
 double类型数字。除数，不能为0。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 余数 
 返回两数相除的余数。结果的正负号与被除数相同。 
 
 
 double最大值（1.7976931348623158e+308） 
 错误（除数不能为0） 
 
 
 脚本示例 

 // 正确的脚本示例：
double a=5.0;
double b=4.0;
double res = Mod(a,b); // res=1.0
// 错误的脚本示例：
string a="abc";
double b=4.0;
double res = Mod(a,b); // 参数类型错误
 

# 1.1.14 左移位

**uint32 >ShiftLeft(uint32 value,int32 shift)**
左移位，高位丢弃，低位补0。
参数名
类型
说明
value
uint32
要左移操作的数字。
shift
int32
左移位数。范围[-2147483648,2147483647]
返回值
说明
左移后的值
返回uint32 类型范围内的值。左移位数为负即按照右移处理,移动的位数超过32位，操作符会对32取模，然后左移。
**脚本示例**
uint32 res = ShiftLeft(10,1); // res=20
// 下面示例中要38对32取模得到6 然后左移6位：
uint32 res = ShiftLeft(11,38); // res=704
// 与上面示例的结果相同
uint32 res = ShiftLeft(11,6); // res=704

### 1.1.14 左移位

 uint32 >ShiftLeft(uint32 value,int32 shift) 

 
 
 
 功能描述 
 
 
 
 左移位，高位丢弃，低位补0。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 value 
 uint32 
 要左移操作的数字。 
 
 
 shift 
 int32 
 左移位数。范围[-2147483648,2147483647] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 左移后的值 
 返回uint32 类型范围内的值。左移位数为负即按照右移处理,移动的位数超过32位，操作符会对32取模，然后左移。 
 
 
 脚本示例 

 uint32 res = ShiftLeft(10,1); // res=20
// 下面示例中要38对32取模得到6 然后左移6位：
uint32 res = ShiftLeft(11,38); // res=704
// 与上面示例的结果相同
uint32 res = ShiftLeft(11,6); // res=704
 

# 1.1.15 右移位

**uint32 >ShiftRight(uint32 value,int32 shift)**
右移位，无符号数，最高位补0。有符号数，右移是“算数移位”。
参数名
类型
说明
value
uint32
要操作的数字。
shift
int32
右移位数。范围[-2147483648,2147483647]
返回值
说明
右移后的值
返回uint32 类型范围内的值。右移位数为负，按照左移处理。
**脚本示例**
uint32 res = ShiftRight(10,1); // res=5

### 1.1.15 右移位

 uint32 >ShiftRight(uint32 value,int32 shift) 

 
 
 
 功能描述 
 
 
 
 右移位，无符号数，最高位补0。有符号数，右移是“算数移位”。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 value 
 uint32 
 要操作的数字。 
 
 
 shift 
 int32 
 右移位数。范围[-2147483648,2147483647] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 右移后的值 
 返回uint32 类型范围内的值。右移位数为负，按照左移处理。 
 
 
 脚本示例 

 uint32 res = ShiftRight(10,1); // res=5
 

# 1.1.16 循环左移位

**uint32 >RollLeft(uint32 value,int32 shift)**
循环左移位，将数字转为二进制然后进行循环移动，将移出的高位放到低位。参考c语言：_crol_(a,b)。
参数名
类型
说明
value
uint32
要操作的数字。
shift
int32
循环左移位数。范围[-2147483648,2147483647]
返回值
说明
循环左移后的值
uint32类型值。
**脚本示例**
uint32 res = RollLeft(10,1); // res=20

### 1.1.16 循环左移位

 uint32 >RollLeft(uint32 value,int32 shift) 

 
 
 
 功能描述 
 
 
 
 循环左移位，将数字转为二进制然后进行循环移动，将移出的高位放到低位。参考c语言： crol (a,b)。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 value 
 uint32 
 要操作的数字。 
 
 
 shift 
 int32 
 循环左移位数。范围[-2147483648,2147483647] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 循环左移后的值 
 uint32类型值。 
 
 
 脚本示例 

 uint32 res = RollLeft(10,1); // res=20
 

# 1.1.17 循环右移位

**uint32 >RollRight(uint32 value,int32 shift)**
循环右移位，将数字转为二进制然后进行循环移动，将移出的低位放到高位。参考c语言：_cror_(a,b)。
参数名
类型
说明
value
uint32
要操作的数字。
shift
int32
循环右移位数。范围[-2147483648,2147483647]
返回值
说明
循环右移后的值
uint32类型值。
**脚本示例**
uint32 res = RollRight(10,1); // res=5

### 1.1.17 循环右移位

 uint32 >RollRight(uint32 value,int32 shift) 

 
 
 
 功能描述 
 
 
 
 循环右移位，将数字转为二进制然后进行循环移动，将移出的低位放到高位。参考c语言： cror (a,b)。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 value 
 uint32 
 要操作的数字。 
 
 
 shift 
 int32 
 循环右移位数。范围[-2147483648,2147483647] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 循环右移后的值 
 uint32类型值。 
 
 
 脚本示例 

 uint32 res = RollRight(10,1); // res=5
 

# 1.1.18 向上舍入

**double >RoundUp(double number ,int64 num_digits)**
远离零值，向上（绝对值增大的方向）舍入数字，参考c++:ceil()。
参数名
类型
说明
number
double
要操作的数字
num_digits
int64
小数位
返回值
说明
向上舍入后的值
double类型值。如果num_digits大于0，则向上舍入到指定小数位，如果等于0，则向上舍入到最接近的整数，如果小于0，则在小数点左侧向上进行舍入。小数位数超出数字位数，处理到最后一位数字
**脚本示例**
RoundUp(3.2,0); // =4 // 将3.2向上舍入，小数位为0
RoundUp(76.9,0); // =77 // 将76.9向上舍入，小数位为0
RoundUp(3.14159,3); // =3.142 // 将3.14159向上舍入，小数位为3
RoundUp(-3.14159,1); // =-3.2 // 将-3.14159向上舍入，小数位为1
RoundUp(31415.92654,-2); // =31500 // 将31415.92654向上舍入到小数点左侧两位

### 1.1.18 向上舍入

 double >RoundUp(double number ,int64 num_digits) 

 
 
 
 功能描述 
 
 
 
 远离零值，向上（绝对值增大的方向）舍入数字，参考c++:ceil()。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 number 
 double 
 要操作的数字 
 
 
 num_digits 
 int64 
 小数位 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 向上舍入后的值 
 double类型值。如果num_digits大于0，则向上舍入到指定小数位，如果等于0，则向上舍入到最接近的整数，如果小于0，则在小数点左侧向上进行舍入。小数位数超出数字位数，处理到最后一位数字 
 
 
 脚本示例 

 RoundUp(3.2,0); // =4 // 将3.2向上舍入，小数位为0
RoundUp(76.9,0); // =77 // 将76.9向上舍入，小数位为0
RoundUp(3.14159,3); // =3.142 // 将3.14159向上舍入，小数位为3
RoundUp(-3.14159,1); // =-3.2 // 将-3.14159向上舍入，小数位为1
RoundUp(31415.92654,-2); // =31500 // 将31415.92654向上舍入到小数点左侧两位
 

# 1.1.19 向下舍入

**double >RoundDown(double number ,int64 num_digits)**
靠近零值，向下（绝对值减小的方向）舍入数字，参考c++:FLOOR()。
参数名
类型
说明
number
double
要操作的数字
num_digits
int64
小数位
返回值
说明
向上舍入后的值
double类型值。如果num_digits大于0，则向下舍入到指定小数位，如果等于0，则向下舍入到最接近的整数，如果小于0，则在小数点左侧向下进行舍入。
**脚本示例**
RoundDown(3.2,0); // =3 //将3.2向下舍入，小数位为0
RoundDown(76.9,0); // =76 //将76.9向下舍入，小数位为0
RoundDown(3.14159,3); // =3.141 //将3.14159向下舍入，小数位为3
RoundDown(-3.14159,1); // =-3.1 //将-3.14159向下舍入，小数位为1
RoundDown(31415.92654,-2); // =31400 //将31415.92654向上舍入到小数点左侧两位

### 1.1.19 向下舍入

 double >RoundDown(double number ,int64 num_digits) 

 
 
 
 功能描述 
 
 
 
 靠近零值，向下（绝对值减小的方向）舍入数字，参考c++:FLOOR()。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 number 
 double 
 要操作的数字 
 
 
 num_digits 
 int64 
 小数位 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 向上舍入后的值 
 double类型值。如果num_digits大于0，则向下舍入到指定小数位，如果等于0，则向下舍入到最接近的整数，如果小于0，则在小数点左侧向下进行舍入。 
 
 
 脚本示例 

 RoundDown(3.2,0); // =3 //将3.2向下舍入，小数位为0
RoundDown(76.9,0); // =76 //将76.9向下舍入，小数位为0
RoundDown(3.14159,3); // =3.141 //将3.14159向下舍入，小数位为3
RoundDown(-3.14159,1); // =-3.1 //将-3.14159向下舍入，小数位为1
RoundDown(31415.92654,-2); // =31400 //将31415.92654向上舍入到小数点左侧两位
 

# 1.1.20 四舍五入

**double >Round(double number ,int64 num_digits)**
按照指定的小数位进行四舍五入。
参数名
类型
说明
number
double
要操作的数字
num_digits
int64
小数位
返回值
说明
四舍五入的值
double类型值。如果num_digits大于0，则四舍五入到指定小数位，如果等于0，则舍入到最接近的整数，如果小于0，则在小数点左侧进行四舍五入。
**脚本示例**
double res = Round(10.25,1); //res=10.3

### 1.1.20 四舍五入

 double >Round(double number ,int64 num_digits) 

 
 
 
 功能描述 
 
 
 
 按照指定的小数位进行四舍五入。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 number 
 double 
 要操作的数字 
 
 
 num_digits 
 int64 
 小数位 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 四舍五入的值 
 double类型值。如果num_digits大于0，则四舍五入到指定小数位，如果等于0，则舍入到最接近的整数，如果小于0，则在小数点左侧进行四舍五入。 
 
 
 脚本示例 

 double res = Round(10.25,1); //res=10.3
 

# 1.2 随机数函数

## 1.2 随机数函数

# 1.2.1 设置随机数种子

**void >SetRandSeed(uint32 seed)**
App的随机数默认种子：本地utc时间的百纳秒，用户也可以自定义随机数种子。
参数名
类型
说明
seed
uint32
自定义随机数种子
**返回值**

返回值
说明

无

**脚本示例**
SetRandSeed(5000);

### 1.2.1 设置随机数种子

 void >SetRandSeed(uint32 seed) 

 
 
 
 功能描述 
 
 
 
 App的随机数默认种子：本地utc时间的百纳秒，用户也可以自定义随机数种子。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 seed 
 uint32 
 自定义随机数种子 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 无 
 
 
 
 
 脚本示例 

 SetRandSeed(5000);
 

# 1.2.2 默认范围内的浮点随机数

**double >Rand()**
返回默认范围内的浮点随机数。
返回值
说明

浮点随机数，范围【-65535.0~65535.0】
**脚本示例**
double res = Rand(); 

### 1.2.2 默认范围内的浮点随机数

 double >Rand() 

 
 
 
 功能描述 
 
 
 
 返回默认范围内的浮点随机数。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 浮点随机数，范围【-65535.0~65535.0】 
 
 
 脚本示例 

 double res = Rand(); 
 

# 1.2.3 自定义范围内的浮点随机数

**double >Rand(double min,double max)**
设置范围，返回浮点随机数。
Min、max的大小输入相反会自动调整
Min、max相差不能低于0.000001
如果失败，调用getLastError()，返回3
参数名
类型
说明
min
double
最小值
max
double
最大值
返回值
说明

浮点随机数，范围【-65535.0~65535.0】
**脚本示例**
Trace(2, "Rand(1.1,100.1):", Rand(1.1,100.1));
Trace(2, "Rand(1.1,1.2):", Rand(1.1,1.2));
Trace(2, "Rand(100.1,100.1):", Rand(100.1,100.1)); // 精度相差小于0.000001，返回100.1
Trace(2, "Rand(100.1,1.1):", Rand(100.1,1.1)); // 自动调整区间为：[1.1,100.1]
Trace(2, "Rand(1.00001,1.00002):", Rand(1.00001,1.00002));
Trace(2, "Rand(1.000001,1.000002):", Rand(1.000001,1.000002)); // 自动调整区间为：[1.000001,1.000002]
Trace(2, "Rand(1.00002,1.00001):", Rand(1.00002,1.00001)); // 自动调整区间为:[1.00001,1.00002]
Trace(2, "Rand(1.000002,1.000001):", Rand(1.000002,1.000001)); // 自动调整区间为:[1.000001,1.000002]
Trace(2, "Rand(1.00000000001,1.00000000002):", Rand(1.00000000001,1.00000000002)); //精度相差小于0.000001，返回1.000000
Trace(2, "Rand(1.00000000002,1.00000000001):", Rand(1.00000000002,1.00000000001)); //精度相差小于0.000001，返回1.000000

### 1.2.3 自定义范围内的浮点随机数

 double >Rand(double min,double max) 

 
 
 
 功能描述 
 
 
 
 设置范围，返回浮点随机数。 
 
 
 Min、max的大小输入相反会自动调整 
 
 
 Min、max相差不能低于0.000001 
 
 
 如果失败，调用getLastError()，返回3 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 min 
 double 
 最小值 
 
 
 max 
 double 
 最大值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 浮点随机数，范围【-65535.0~65535.0】 
 
 
 脚本示例 

 Trace(2, "Rand(1.1,100.1):", Rand(1.1,100.1));
Trace(2, "Rand(1.1,1.2):", Rand(1.1,1.2));
Trace(2, "Rand(100.1,100.1):", Rand(100.1,100.1)); // 精度相差小于0.000001，返回100.1
Trace(2, "Rand(100.1,1.1):", Rand(100.1,1.1)); // 自动调整区间为：[1.1,100.1]
Trace(2, "Rand(1.00001,1.00002):", Rand(1.00001,1.00002));
Trace(2, "Rand(1.000001,1.000002):", Rand(1.000001,1.000002)); // 自动调整区间为：[1.000001,1.000002]
 
 Trace(2, "Rand(1.00002,1.00001):", Rand(1.00002,1.00001)); // 自动调整区间为:[1.00001,1.00002]
Trace(2, "Rand(1.000002,1.000001):", Rand(1.000002,1.000001)); // 自动调整区间为:[1.000001,1.000002]
 
 Trace(2, "Rand(1.00000000001,1.00000000002):", Rand(1.00000000001,1.00000000002)); //精度相差小于0.000001，返回1.000000
Trace(2, "Rand(1.00000000002,1.00000000001):", Rand(1.00000000002,1.00000000001)); //精度相差小于0.000001，返回1.000000
 

# 1.2.4 默认范围内的整数随机数

**int32 >RandInt()**
返回默认范围内的整数随机数。
返回值
说明

整数随机数，范围【-2147483648 ~ 2147483647】
**脚本示例**
Trace(2, "RandInt():",RandInt());

### 1.2.4 默认范围内的整数随机数

 int32 >RandInt() 

 
 
 
 功能描述 
 
 
 
 返回默认范围内的整数随机数。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 整数随机数，范围【-2147483648 ~ 2147483647】 
 
 
 脚本示例 

 Trace(2, "RandInt():",RandInt());
 

# 1.2.5 自定义范围内的整数随机数

**int32 >RandInt(int32 min,int32 max)**
设置范围，返回整数随机数。
Min、max的大小输入相反会自动调整。
参数名
类型
说明
min
int32
最小值
max
int32
最大值
返回值
说明

整数随机数
**脚本示例**
Trace(2, "RandInt(1,100):",RandInt(1,100));
Trace(2, "RandInt(100,100):",RandInt(100,100)); // 返回100
Trace(2, "RandInt(100,1):",RandInt(100,1)); // 自动调整区间为:[1,100]

### 1.2.5 自定义范围内的整数随机数

 int32 >RandInt(int32 min,int32 max) 

 
 
 
 功能描述 
 
 
 
 设置范围，返回整数随机数。 
 
 
 Min、max的大小输入相反会自动调整。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 min 
 int32 
 最小值 
 
 
 max 
 int32 
 最大值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 整数随机数 
 
 
 脚本示例 

 Trace(2, "RandInt(1,100):",RandInt(1,100));
Trace(2, "RandInt(100,100):",RandInt(100,100)); // 返回100
Trace(2, "RandInt(100,1):",RandInt(100,1)); // 自动调整区间为:[1,100]
 

# 1.3 字符串函数

## 1.3 字符串函数

# 1.3.1 字符串转double

**double >RealParse(string str)** 
字符串转double。
参数名
类型
说明
str
string
要转换为数字的字符串。只有string类型才可以，其他类型返回0.0
返回值
说明
转换后的数字
只有数字字符会得到对应数值，超出目标范围要报错。程序处理可以从前往后读，若遇到不是数字的类型停止转换，比如“1a”,可以转为1。

值为“ 1234 ”字符串执行方法返回1234；
值为“ 12 34 ”的字符串方法返回12；
值为“123”的字符串执行方法返回123；
值为“-123”的字符串执行方法返回-123；
0.0
包含其他非数字字符，会转换失败，返回0.0，比如，
值为“”的字符串执行方法返回0.0；
值为“ ”的字符串执行方法返回0.0；
值为“FF”的字符串执行方法返回0.0；
**脚本示例**
string str ="1.25";
double res = RealParse(str); //res=1.25

### 1.3.1 字符串转double

 double >RealParse(string str) 

 
 
 
 功能描述 
 
 
 
 字符串转double。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 str 
 string 
 要转换为数字的字符串。只有string类型才可以，其他类型返回0.0 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 转换后的数字 
 只有数字字符会得到对应数值，超出目标范围要报错。程序处理可以从前往后读，若遇到不是数字的类型停止转换，比如“1a”,可以转为1。 
 
 
 
 值为“ 1234 ”字符串执行方法返回1234；
值为“ 12 34 ”的字符串方法返回12；
值为“123”的字符串执行方法返回123；
值为“-123”的字符串执行方法返回-123； 
 
 
 0.0 
 包含其他非数字字符，会转换失败，返回0.0，比如，
值为“”的字符串执行方法返回0.0；
值为“ ”的字符串执行方法返回0.0；
值为“FF”的字符串执行方法返回0.0； 
 
 
 脚本示例 

 string str ="1.25";
double res = RealParse(str); //res=1.25
 

# 1.3.2 字符串转整数

**int64 >IntParse(string str,int8 radix)**
将字符串以radix[2-36]进制编码方式转为十进制返回，参考JS:ParseInt
参数名
类型
说明
str
string
要转换为十进制的字符串。
radix
int8
编码方式，比如2，8，10，16等，范围[2-36]
返回值
说明
转换后的数字
忽略字符串前面的空格 缩进等。十进制整数，负号可以识别，小数点后直接舍去。比如IntParse(“-a.1”,16)//输出-10
0
如果字符串不合理，返回0。不合理字符串格式：
1、含有radix指定进制不识别字符，比如radix为16，str含"Z"字符。
2、含其他不识别字符，比如中间空格，缩进等。
0
radix超出[2-36] 范围
**脚本示例**
string str = “ABC”;
int8 rad = 16;
int64 res = IntParse(str,rad); // res=2748

### 1.3.2 字符串转整数

 int64 >IntParse(string str,int8 radix) 

 
 
 
 功能描述 
 
 
 
 将字符串以radix[2-36]进制编码方式转为十进制返回，参考JS:ParseInt 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 str 
 string 
 要转换为十进制的字符串。 
 
 
 radix 
 int8 
 编码方式，比如2，8，10，16等，范围[2-36] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 转换后的数字 
 忽略字符串前面的空格 缩进等。十进制整数，负号可以识别，小数点后直接舍去。比如IntParse(“-a.1”,16)//输出-10 
 
 
 0 
 如果字符串不合理，返回0。不合理字符串格式：
1、含有radix指定进制不识别字符，比如radix为16，str含"Z"字符。
2、含其他不识别字符，比如中间空格，缩进等。 
 
 
 0 
 radix超出[2-36] 范围 
 
 
 脚本示例 

 string str = “ABC”;
int8 rad = 16;
int64 res = IntParse(str,rad); // res=2748
 

# 1.3.3 字符串转正整数

**uint64 >UintParse(string str,int8 radix)**
将字符串以radix[2-36]进制编码方式转为十进制返回，参考JS:ParseUint
参数名
类型
说明
str
string
要转换为十进制的字符串。
radix
int8
编码方式，比如2，8，10，16等，范围[2-36]
返回值
说明
转换后的数字
忽略字符串前面的空格 缩进等。十进制正整数，负号可以识别，有小数点直接返0。比如UintParse(“-123”,10)//输出18446744073709551493
0
如果字符串不合理，返回0。不合理字符串格式：
1、含有radix指定进制不识别字符，比如radix为16，str含"Z"字符。
2、含其他不识别字符，比如中间空格，缩进等。
0
radix超出[2-36] 范围
**脚本示例**
string str = “12345”;
int8 rad = 10;
uint64 res = UintParse(str,rad); //res=12345

### 1.3.3 字符串转正整数

 uint64 >UintParse(string str,int8 radix) 

 
 
 
 功能描述 
 
 
 
 将字符串以radix[2-36]进制编码方式转为十进制返回，参考JS:ParseUint 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 str 
 string 
 要转换为十进制的字符串。 
 
 
 radix 
 int8 
 编码方式，比如2，8，10，16等，范围[2-36] 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 转换后的数字 
 忽略字符串前面的空格 缩进等。十进制正整数，负号可以识别，有小数点直接返0。比如UintParse(“-123”,10)//输出18446744073709551493 
 
 
 0 
 如果字符串不合理，返回0。不合理字符串格式：
1、含有radix指定进制不识别字符，比如radix为16，str含"Z"字符。
2、含其他不识别字符，比如中间空格，缩进等。 
 
 
 0 
 radix超出[2-36] 范围 
 
 
 脚本示例 

 string str = “12345”;
int8 rad = 10;
uint64 res = UintParse(str,rad); //res=12345
 

# 1.3.4 实数转字符串

**string >StringFromReal(float real, int8 precision,string type)**
实数转成一个字符串，失败，返回空字符串。
参数名
类型
说明
real
float
要转换为字符串的实数
precision
int8
保留小数位: string >StringFromReal(float real, int8 precision,string type) 

 
 
 
 功能描述 
 
 
 
 实数转成一个字符串，失败，返回空字符串。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 real 
 float 
 要转换为字符串的实数 
 
 
 precision 
 int8 
 保留小数位: <0、=0：固定为按6位小数位输出。其他数字：表示截断（截断时进行四舍五入）后保留的小数位数。若转换的实数是整数，则此参数对应的是整数小数点后面的0的个数。如果需要将浮点数转成纯整数，没有小数位输出，需要使用StringFromInt()函数。 
 
 
 type 
 string 
 转换类型 ：r\e\E。"r"：按浮点数显示。"e"：按小写"e"的指数制显示。"E"：按大写"E"的指数制显示。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 字符串 
 字符串 
 
 
 转换失败，空串 
 参数3为不识别格式 
 
 
 错误值（不符合预期的值） 
 超出实数范围 
 
 
 默认保留6位 
 参数2<=0 
 
 
 脚本示例 

 string res = StringFromReal(263.355, 2,"r"); // res="263.36"
string res = StringFromReal(263.55, 3,"E"); // res="2.636E+02"
string res = StringFromReal(263.355, 2,"e"); // res="2.63e+02"
 

# 1.3.5 整数转字符串

**string >StringFromInt(int64 nInteger,int8 nbase)**
将整数值转换为进制形式的字符串，参考C语言：itoa()函数，不是标准库。
遵循大部分主流语言标准，按小写返回的性能也更好，字母按小写返回。
参数名
类型
说明
nInteger
int64
要转换为字符串的整数值
nbase
int8
进制类型[2-36]之间，常用进制数：2,8,10,16超出范围其他值。
返回值
说明
字符串
[2-36]进制转换的字符串,字母按小写返回。
按照10进制处理
nbase超出[2-36]范围
错误值
nInteger超出int64范围
**脚本示例**
string res = StringFromInt(68,10); // res="68"
string res = StringFromInt(68,16); // res="44"
string res = StringFromInt(68,32); // res="24"

### 1.3.5 整数转字符串

 string >StringFromInt(int64 nInteger,int8 nbase) 

 
 
 
 功能描述 
 
 
 
 将整数值转换为进制形式的字符串，参考C语言：itoa()函数，不是标准库。 
 
 
 遵循大部分主流语言标准，按小写返回的性能也更好，字母按小写返回。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 nInteger 
 int64 
 要转换为字符串的整数值 
 
 
 nbase 
 int8 
 进制类型[2-36]之间，常用进制数：2,8,10,16超出范围其他值。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 字符串 
 [2-36]进制转换的字符串,字母按小写返回。 
 
 
 按照10进制处理 
 nbase超出[2-36]范围 
 
 
 错误值 
 nInteger超出int64范围 
 
 
 脚本示例 

 string res = StringFromInt(68,10); // res="68"
string res = StringFromInt(68,16); // res="44"
string res = StringFromInt(68,32); // res="24"
 

# 1.3.6 正整数转字符串

**string >StringFromUint(uint64 nInteger,int8 nbase)**
将正整数值转换为进制形式的字符串，参考C语言：itoa()函数，不是标准库。
遵循大部分主流语言标准，按小写返回的性能也更好，字母按小写返回。
参数名
类型
说明
nInteger
uint64
要转换为字符串的正整数值
nbase
int8
进制类型[2-36]之间，常用进制数：2,8,10,16超出范围其他值。
返回值
说明
字符串
[2-36]进制转换的字符串,字母按小写返回。
按照10进制处理
nbase超出[2-36]范围
错误值
nInteger超出uint64范围
**脚本示例**
string res = StringFromUint(68,10); // res="68"
string res = StringFromUint(68,16); // res="44"
string res = StringFromUint(68,32); // res="24"

### 1.3.6 正整数转字符串

 string >StringFromUint(uint64 nInteger,int8 nbase) 

 
 
 
 功能描述 
 
 
 
 将正整数值转换为进制形式的字符串，参考C语言：itoa()函数，不是标准库。 
 
 
 遵循大部分主流语言标准，按小写返回的性能也更好，字母按小写返回。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 nInteger 
 uint64 
 要转换为字符串的正整数值 
 
 
 nbase 
 int8 
 进制类型[2-36]之间，常用进制数：2,8,10,16超出范围其他值。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 字符串 
 [2-36]进制转换的字符串,字母按小写返回。 
 
 
 按照10进制处理 
 nbase超出[2-36]范围 
 
 
 错误值 
 nInteger超出uint64范围 
 
 
 脚本示例 

 string res = StringFromUint(68,10); // res="68"
string res = StringFromUint(68,16); // res="44"
string res = StringFromUint(68,32); // res="24"
 

# 1.4 JSON函数

## 1.4 JSON函数

# 1.4.1 JSON字符串转JSONObject

**JSONObject >JSONObjectParse(string jsonStr)**
解析JSON字符串，将JSON字符串转为JSONObject类型。
参数名
类型
说明
jsonStr
string
JSON格式字符串，要求是JSON对象格式的JSON字符串，即首尾分别为“{“\”}”的JSON格式的字符串
返回值
说明
JSONObject
JSONObject类型的数据
**脚本示例**
string name="App1";
string js="{\"name\":\""+name+"\"}";
JSONObject json1 =JSONObjectParse(js);
Trace(2, "js=", js); // js={"name":"App1"}
String json1name=json1.name;
Trace(2, "json1.name=", json1name); // json1.name=App1
string a="{\"arr\":[\"a\",1],\"info\": [\"age\",\"sex\",14,1]}";
JSONObject b=JSONObjectParse(a);
string c=Typeof(b);
Trace(2, "TransForm_JSONObjectParse:", c); // TransForm_JSONObjectParse:JSONObject

### 1.4.1 JSON字符串转JSONObject

 JSONObject >JSONObjectParse(string jsonStr) 

 
 
 
 功能描述 
 
 
 
 解析JSON字符串，将JSON字符串转为JSONObject类型。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 jsonStr 
 string 
 JSON格式字符串，要求是JSON对象格式的JSON字符串，即首尾分别为“{“\”}”的JSON格式的字符串 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 JSONObject 
 JSONObject类型的数据 
 
 
 脚本示例 

 string name="App1";
string js="{\"name\":\""+name+"\"}";
JSONObject json1 =JSONObjectParse(js);
Trace(2, "js=", js); // js={"name":"App1"}
String json1name=json1.name;
Trace(2, "json1.name=", json1name); // json1.name=App1
 
 string a="{\"arr\":[\"a\",1],\"info\": [\"age\",\"sex\",14,1]}";
JSONObject b=JSONObjectParse(a);
string c=Typeof(b);
Trace(2, "TransForm_JSONObjectParse:", c); // TransForm_JSONObjectParse:JSONObject
 

# 1.4.2 Var类型转JSONObject

**JSONObject >ConvertToJsonObject(var var1)**
Var类型转JSONObject。
参数名
类型
说明
var1
var
任意元语言类型
返回值
说明
JSONObject
JSONObject类型的数据
**脚本示例**
dateTime dt = Now();
jsonObject temp = ConvertToJsonObject(dt);

### 1.4.2 Var类型转JSONObject

 JSONObject >ConvertToJsonObject(var var1) 

 
 
 
 功能描述 
 
 
 
 Var类型转JSONObject。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 var1 
 var 
 任意元语言类型 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 JSONObject 
 JSONObject类型的数据 
 
 
 脚本示例 

 dateTime dt = Now();
jsonObject temp = ConvertToJsonObject(dt);
 

# 1.4.3 Var类型转JSON字符串

**string >ConvertToJsonString(var var1,bool var2)**
Var类型转JSON字符串。
参数名
类型
说明
var1
var
任意元语言类型
var2
bool
可省，默认为false；
true：表示多行输出，多行输出耗性能
false：表示单行输出
返回值
说明
string
JSON格式字符串
**脚本示例**
Trace(2, "ConvertToJsonString=", ConvertToJsonString(now())); // datatime转字符串
Trace(2, "ConvertToJsonString=", ConvertToJsonString("1")); 
Trace(2, "ConvertToJsonString=", ConvertToJsonString(1)); 
string strConvert1 = "1";
Trace(2, "ConvertToJsonString=", ConvertToJsonString(strConvert1)); 
JSONObject json = {
 "name": "xiaozhang",
 "age" : 18,
 "sex" :"nan",
 "level" :{
 "idx":"1"
 },
 "arr":[1,2,3,45,5]};
Trace(2, "ConvertToJsonString=", ConvertToJsonString(json));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["name"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["age"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["level"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["arr"]));
JSONObject json = {
 "name": "xiaozhang",
 "age" : 18,
 "sex" :"nan",
 "level" :{
 "idx":"1"
 },
 "arr":[1,2,3,45,5]};
int age1 = json.age;
Trace(2, "18=", age1);
Trace(2, "18=:", json.age);
Trace(2, "str1=", ConvertToJsonString(json));
json["age"] = 12;
Trace(2, "12=", json.age);
Trace(2, "str2=", ConvertToJsonString(json));
stringMap str1;
str1.a1="str1a1";
str1.a2="str1a2";
str1.a3="str1a3";
str1.a4="str1a4";
str1.a5="str1a5";
Trace(2, "str1-true=", ConvertToJsonString(str1,true));
/*true：多行输出
str1-true={
 "keyType" : "global@string",
 "valueType" : "global@var",
 "map" : {
 "a1" : "str1a1",
 "a2" : "str1a2",
 "a3" : "str1a3",
 "a4" : "str1a4",
 "a5" : "str1a5" 
 } 
}
*/
Trace(2, "str1-false=", ConvertToJsonString(str1,false));
/*false：单行输出
str1-false={"keyType":"global@string","valueType":"global@var","map":{"a1":"str1a1","a2":"str1a2","a3":"str1a3","a4":"str1a4","a5":"str1a5"}}
*/

### 1.4.3 Var类型转JSON字符串

 string >ConvertToJsonString(var var1,bool var2) 

 
 
 
 功能描述 
 
 
 
 Var类型转JSON字符串。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 var1 
 var 
 任意元语言类型 
 
 
 var2 
 bool 
 可省，默认为false；
true：表示多行输出，多行输出耗性能
false：表示单行输出 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 JSON格式字符串 
 
 
 脚本示例 

 Trace(2, "ConvertToJsonString=", ConvertToJsonString(now())); // datatime转字符串
Trace(2, "ConvertToJsonString=", ConvertToJsonString("1")); 
Trace(2, "ConvertToJsonString=", ConvertToJsonString(1)); 
string strConvert1 = "1";
Trace(2, "ConvertToJsonString=", ConvertToJsonString(strConvert1)); 
JSONObject json = {
 "name": "xiaozhang",
 "age" : 18,
 "sex" :"nan",
 "level" :{
 "idx":"1"
 },
 "arr":[1,2,3,45,5]};
Trace(2, "ConvertToJsonString=", ConvertToJsonString(json));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["name"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["age"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["level"]));
Trace(2, "ConvertToJsonString3=", ConvertToJsonString(json["arr"]));
 
 JSONObject json = {
 "name": "xiaozhang",
 "age" : 18,
 "sex" :"nan",
 "level" :{
 "idx":"1"
 },
 "arr":[1,2,3,45,5]};
int age1 = json.age;
Trace(2, "18=", age1);
Trace(2, "18=:", json.age);
Trace(2, "str1=", ConvertToJsonString(json));
json["age"] = 12;
Trace(2, "12=", json.age);
Trace(2, "str2=", ConvertToJsonString(json));
 
 stringMap<var> str1;
str1.a1="str1a1";
str1.a2="str1a2";
str1.a3="str1a3";
str1.a4="str1a4";
str1.a5="str1a5";
Trace(2, "str1-true=", ConvertToJsonString(str1,true));
/*true：多行输出
str1-true={
 "keyType" : "global@string",
 "valueType" : "global@var",
 "map" : {
 "a1" : "str1a1",
 "a2" : "str1a2",
 "a3" : "str1a3",
 "a4" : "str1a4",
 "a5" : "str1a5" 
 } 
}
*/
Trace(2, "str1-false=", ConvertToJsonString(str1,false));
/*false：单行输出
str1-false={"keyType":"global@string","valueType":"global@var","map":{"a1":"str1a1","a2":"str1a2","a3":"str1a3","a4":"str1a4","a5":"str1a5"}}
*/
 

# 1.5 日期时间函数

## 1.5 日期时间函数

# 1.5.1 DateTime()

**DateTime >DateTime(int16 year,int8 month,int8 day,int8 hour,int8 minute,int8 second,int16 millisecond,int16 timezoneOffset)**
根据源日期时间参数+偏移量，输出日期时间，输出时间=源时间+timezoneOffset。
DateTime 时间从1601月1日1日开始，1601年前的时间没有意义。
参数名
类型
说明
year
int16
源时间的Year：1601-9999
month
int8
源时间的Month：1-12
day
int8
源时间的Day：1-31
hour
int8
源时间的Hour：0-23
minute
int8
源时间的Minute：0-59
second
int8
源时间的Second：0-59
millisecond
int16
源时间的Millisecond：000-999
timezoneOffset
int16
输出时间与源时间之间的偏移量，单位：分钟，可省略。
参数
返回值
timezoneOffset=0
输出时间=源时间
timezoneOffset省略
函数自动计算本地时区和格林威治时区之间的偏移作为默认的timezoneOffset，输出时间=源时间+（本地时区-格林威治时区）
timezoneOffset不为0
输出时间=源时间+timezoneOffset
**脚本示例**
// 示例1
DateTime time=DateTime(2020,5,8,12,15,20,25,0);
string timeStr1=time.toString("Fms",0);
string timeStr2=time.toString("Fms",-480);
string timeStr3=time.toString("Fms");
// 输出如下：
// timeStr1="2020-5-8 12:15:20.025"
// timeStr2="2020-5-8 4:15:20.025"
// timeStr3="2020-5-8 20:15:20.025"
// 示例2
DateTime dtime;
int16 year=2021;
int8 month=12;
int8 day=31;
int8 hour=23;
int8 min=59;
int8 sec=59;
int16 msec=999;
dtime=DateTime(year,month,day,hour,min,sec,msec,0); // dtime=“2021-12-31 23:59:59.999”
// 错误示例
DateTime dtime;
string year="9999";
int8 month=12;
int8 day=31;
int8 hour=23;
int min=59;
int sec=59;
int8 msec=999;
dtime=DateTime(year,month,day,hour,min,sec,msec); // 编译失败，参数类型错误

### 1.5.1 DateTime()

 DateTime >DateTime(int16 year,int8 month,int8 day,int8 hour,int8 minute,int8 second,int16 millisecond,int16 timezoneOffset) 

 
 
 
 功能描述 
 
 
 
 根据源日期时间参数+偏移量，输出日期时间，输出时间=源时间+timezoneOffset。 
 
 
 DateTime 时间从1601月1日1日开始，1601年前的时间没有意义。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 year 
 int16 
 源时间的Year：1601-9999 
 
 
 month 
 int8 
 源时间的Month：1-12 
 
 
 day 
 int8 
 源时间的Day：1-31 
 
 
 hour 
 int8 
 源时间的Hour：0-23 
 
 
 minute 
 int8 
 源时间的Minute：0-59 
 
 
 second 
 int8 
 源时间的Second：0-59 
 
 
 millisecond 
 int16 
 源时间的Millisecond：000-999 
 
 
 timezoneOffset 
 int16 
 输出时间与源时间之间的偏移量，单位：分钟，可省略。 
 
 
 
 
 
 返回值 
 
 
 
 
 参数 
 返回值 
 
 
 timezoneOffset=0 
 输出时间=源时间 
 
 
 timezoneOffset省略 
 函数自动计算本地时区和格林威治时区之间的偏移作为默认的timezoneOffset，输出时间=源时间+（本地时区-格林威治时区） 
 
 
 timezoneOffset不为0 
 输出时间=源时间+timezoneOffset 
 
 
 脚本示例 

 // 示例1
DateTime time=DateTime(2020,5,8,12,15,20,25,0);
string timeStr1=time.toString("Fms",0);
string timeStr2=time.toString("Fms",-480);
string timeStr3=time.toString("Fms");
// 输出如下：
// timeStr1="2020-5-8 12:15:20.025"
// timeStr2="2020-5-8 4:15:20.025"
// timeStr3="2020-5-8 20:15:20.025"
 
 // 示例2
DateTime dtime;
int16 year=2021;
int8 month=12;
int8 day=31;
int8 hour=23;
int8 min=59;
int8 sec=59;
int16 msec=999;
dtime=DateTime(year,month,day,hour,min,sec,msec,0); // dtime=“2021-12-31 23:59:59.999”
 
 // 错误示例
DateTime dtime;
string year="9999";
int8 month=12;
int8 day=31;
int8 hour=23;
int min=59;
int sec=59;
int8 msec=999;
dtime=DateTime(year,month,day,hour,min,sec,msec); // 编译失败，参数类型错误
 

# 1.5.2 DateTimeParse()

**DateTime >DateTimeParse(string str,int16timezoneOffset)**
将日期时间字符串转为DateTime，输出时间=源字符串时间+timezoneOffset。
参数名
类型
说明
str
string
日期时间字符串，格式规范:yyyy[-M[-d[h[:m[:s[.ff]]]]]] 或 yyyy[-MM[-dd[ hh[:mm[:ss[.ff]]]]]] （方括号中的部分可省略）
格式元素说明：在给定时间字符串参数时”2021-1-2“和”2021-01-2“都是允许的
yyyy: 年，不可省略
M:月，范围介于1到12之间，可省，省略时默认为1
d:天，范围介于1到31之间，可省，省略时默认为1
h:小时，范围介于0到23之间，可省，省略时默认为0
m:分钟，范围介于0到59之间，可省，省略时默认为0
s: 秒，范围介于0到59之间，可省，省略时默认为0
ff:可选的秒的小数部分，由3到7个十进制数字组成，可省，省略时默认为000。
参数”2020-12-12”，默认等于”2020-12-12 00:00:00.000”，参数”2020-12-12 12”，默认等于”2020-12-12 12:00:00.000”……
timezoneOffset
int16
输出时间与源字符串时间之间的偏移量，单位：分钟，可省略。
参数
返回值
timezoneOffset=0
输出时间=源字符串时间
timezoneOffset省略
函数自动计算格林威治时区和本地时区之间的偏移作为默认的timezoneOffset，输出时间=源字符串时间+（格林威治时区-本地时区）
timezoneOffset不为0
输出时间=源字符串时间+timezoneOffset
1601-01-01 00:00:0
字符串不合法返回最小时间：1601-01-01 00:00:0
**脚本示例**
// 示例1
DateTime timeparse;
string str="2021-1-1 0:0:0.000";
timeparse =DateTimeParse(str,0); // timeparse： "2021-1-1 0:0:0.000"
// 示例2
DateTime timeparse;
string str="";
timeparse =DateTimeParse(str,0); // timeparse：1601-01-01 00:00:0
// 示例3
DateTime dt1= DateTimeParse("2022-04-06 13:00:0.000");
uint64 tick=dt1.Ticks;
Trace(2, "tick=", tick); // tick=132936948000000000
datetime dt2;
dt2=DateTimeByTicks(tick);
Trace(2, "dt2=",dt2.toString("Fms")); // dt2="2022-04-06 13:00:0.000"
// 示例4
DateTime dt1= DateTimeParse("2022-04-06 13:00:0.000",0);
uint64 tick=dt1.Ticks;
Trace(2, "tick=", tick); // tick=132936948000000000
datetime dt2;
dt2=DateTimeByTicks(tick);
Trace(2, "dt2=", dt2.toString("Fms")); // dt2="2022-04-06 21:00:0.000"
// 示例5
DateTime timeparse;
string str="2024";
timeparse =DateTimeParse(str,0); 
string b=timeparse.toString("Fms",480);
Trace(2, "b=", b); // b="2024-1-1 8:0:0.000"

### 1.5.2 DateTimeParse()

 DateTime >DateTimeParse(string str,int16timezoneOffset) 

 
 
 
 功能描述 
 
 
 
 将日期时间字符串转为DateTime，输出时间=源字符串时间+timezoneOffset。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 str 
 string 
 日期时间字符串，格式规范:yyyy[-M[-d[h[:m[:s[.ff]]]]]] 或 yyyy[-MM[-dd[ hh[:mm[:ss[.ff]]]]]] （方括号中的部分可省略）
格式元素说明：在给定时间字符串参数时”2021-1-2“和”2021-01-2“都是允许的
yyyy: 年，不可省略
M:月，范围介于1到12之间，可省，省略时默认为1
d:天，范围介于1到31之间，可省，省略时默认为1
h:小时，范围介于0到23之间，可省，省略时默认为0
m:分钟，范围介于0到59之间，可省，省略时默认为0
s: 秒，范围介于0到59之间，可省，省略时默认为0
ff:可选的秒的小数部分，由3到7个十进制数字组成，可省，省略时默认为000。
参数”2020-12-12”，默认等于”2020-12-12 00:00:00.000”，参数”2020-12-12 12”，默认等于”2020-12-12 12:00:00.000”…… 
 
 
 timezoneOffset 
 int16 
 输出时间与源字符串时间之间的偏移量，单位：分钟，可省略。 
 
 
 
 
 
 返回值 
 
 
 
 
 参数 
 返回值 
 
 
 timezoneOffset=0 
 输出时间=源字符串时间 
 
 
 timezoneOffset省略 
 函数自动计算格林威治时区和本地时区之间的偏移作为默认的timezoneOffset，输出时间=源字符串时间+（格林威治时区-本地时区） 
 
 
 timezoneOffset不为0 
 输出时间=源字符串时间+timezoneOffset 
 
 
 1601-01-01 00:00:0 
 字符串不合法返回最小时间：1601-01-01 00:00:0 
 
 
 脚本示例 

 // 示例1
DateTime timeparse;
string str="2021-1-1 0:0:0.000";
timeparse =DateTimeParse(str,0); // timeparse： "2021-1-1 0:0:0.000"
 
 // 示例2
DateTime timeparse;
string str="";
timeparse =DateTimeParse(str,0); // timeparse：1601-01-01 00:00:0
 
 // 示例3
DateTime dt1= DateTimeParse("2022-04-06 13:00:0.000");
uint64 tick=dt1.Ticks;
Trace(2, "tick=", tick); // tick=132936948000000000
datetime dt2;
dt2=DateTimeByTicks(tick);
Trace(2, "dt2=",dt2.toString("Fms")); // dt2="2022-04-06 13:00:0.000"
 
 // 示例4
DateTime dt1= DateTimeParse("2022-04-06 13:00:0.000",0);
uint64 tick=dt1.Ticks;
Trace(2, "tick=", tick); // tick=132936948000000000
datetime dt2;
dt2=DateTimeByTicks(tick);
Trace(2, "dt2=", dt2.toString("Fms")); // dt2="2022-04-06 21:00:0.000"
 
 // 示例5
DateTime timeparse;
string str="2024";
timeparse =DateTimeParse(str,0); 
string b=timeparse.toString("Fms",480);
Trace(2, "b=", b); // b="2024-1-1 8:0:0.000"
 

# 1.5.3 DateTimeByTicks()

**DateTime >DateTimeByTicks(uint64 ticks)**
将ticks转为DateTime。
DateTime 时间从1601月1日1日开始，1601年前的时间没有意义。
参数名
类型
说明
ticks
uint64
100ns数
参数
返回值
ticks>0
>1601-1-1 0:0:0.000的日期时间
ticks DateTime >DateTimeByTicks(uint64 ticks) 

 
 
 
 功能描述 
 
 
 
 将ticks转为DateTime。 
 
 
 DateTime 时间从1601月1日1日开始，1601年前的时间没有意义。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 ticks 
 uint64 
 100ns数 
 
 
 
 
 
 返回值 
 
 
 
 
 参数 
 返回值 
 
 
 ticks>0 
 >1601-1-1 0:0:0.000的日期时间 
 
 
 ticks<=0 
 1601-1-1 0:0:0.000 
 
 
 脚本示例 

 // 示例1
uint64 tick=864000000000;
DateTime dt;
dt =DateTimeByTicks(tick); 
Trace(2, "dt=", dt.toString("Fms")); // dt="1601-1-2 8:0:0.000"
 
 // 错误示例
string tick="864000000000";
DateTime dt;
dt =DateTimeByTicks(tick); // 编译失败，参数类型错误
 

# 1.5.4 Now()

**DateTime >Now()**
获取当前时间。时间为utc时间
**脚本示例**
// 示例1
DateTime currentTime = Now();
string str=currentTime.toString("Fms",480); // str="2021-3-4 13:48:34.67"
// 错误示例
DateTime currentTime = Now(2021); // 编译失败，方法未找到

### 1.5.4 Now()

 DateTime >Now() 

 
 
 
 功能描述 
 
 
 
 获取当前时间。时间为utc时间 
 
 
 脚本示例 

 // 示例1
DateTime currentTime = Now();
string str=currentTime.toString("Fms",480); // str="2021-3-4 13:48:34.67"
 
 // 错误示例
DateTime currentTime = Now(2021); // 编译失败，方法未找到
 

# 1.5.5 IsLeapYear()

**bool >IsLeapYear(int16 year)**
判断是否是闰年。
参数名
类型
说明
year
int16
年：1601-9999
参数
返回值
year为非闰年
0=不是闰年
year为闰年
1=是闰年
**脚本示例**
// 示例1
bool byear;
byear =IsLeapYear(2020); // byear=1
// 错误示例
bool byear;
byear =IsLeapYear(); // 编译失败

### 1.5.5 IsLeapYear()

 bool >IsLeapYear(int16 year) 

 
 
 
 功能描述 
 
 
 
 判断是否是闰年。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 year 
 int16 
 年：1601-9999 
 
 
 
 
 
 返回值 
 
 
 
 
 参数 
 返回值 
 
 
 year为非闰年 
 0=不是闰年 
 
 
 year为闰年 
 1=是闰年 
 
 
 脚本示例 

 // 示例1
bool byear;
byear =IsLeapYear(2020); // byear=1
 
 // 错误示例
bool byear;
byear =IsLeapYear(); // 编译失败
 

# 1.5.6 GetLocalTimezoneOffset()

**int16 >GetLocalTimezoneOffset()**
获取本机时区与UTC时区之间的时间差，以分钟为单位。
返回时间差以分钟计，而不是以小时计，原因是某些国家所占有的时区还不到一小时的间隔。
如果本机时区存在夏令时制，在夏天、非夏天，获取的差值不一样。
返回值
说明
负时间差
本机时区在UTC时区之前
正时间差
本机时区在UTC时区之后
**脚本示例**
// 示例1
int16 t;
t=GetLocalTimezoneOffset(); // t=480
// 错误示例
int16 tim;
tim=GetlocalTimezoneOffset(2021); // 编译失败

### 1.5.6 GetLocalTimezoneOffset()

 int16 >GetLocalTimezoneOffset() 

 
 
 
 功能描述 
 
 
 
 获取本机时区与UTC时区之间的时间差，以分钟为单位。 
 
 
 返回时间差以分钟计，而不是以小时计，原因是某些国家所占有的时区还不到一小时的间隔。 
 
 
 如果本机时区存在夏令时制，在夏天、非夏天，获取的差值不一样。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 负时间差 
 本机时区在UTC时区之前 
 
 
 正时间差 
 本机时区在UTC时区之后 
 
 
 脚本示例 

 // 示例1
int16 t;
t=GetLocalTimezoneOffset(); // t=480
 
 // 错误示例
int16 tim;
tim=GetlocalTimezoneOffset(2021); // 编译失败
 

# 1.6 时间间隔函数

## 1.6 时间间隔函数

# 1.6.1 TimeSpan()

**TimeSpan >TimeSpan(int32 day,int32 hour,int32 minute,int32 second,int32 millisecond)**
根据输入参数得到一个TimeSpan。参数可为负，表示负的时间间隔。
参数名
类型
说明
day
int32
时间间隔天的整数
hour
int32
时间间隔小时的整数
minute
int32
时间间隔分钟的整数
second
int32
时间间隔秒的整数
millisecond
int32
时间间隔毫秒的整数
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
ts=TimeSpan(4,1,4,2,20);
string s3=ts.toString(); // s3="4.1:4:2.20"
// 错误示例
TimeSpan ts;
ts=TimeSpan("4","1",4,2,20); // 编译失败

### 1.6.1 TimeSpan()

 TimeSpan >TimeSpan(int32 day,int32 hour,int32 minute,int32 second,int32 millisecond) 

 
 
 
 功能描述 
 
 
 
 根据输入参数得到一个TimeSpan。参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 day 
 int32 
 时间间隔天的整数 
 
 
 hour 
 int32 
 时间间隔小时的整数 
 
 
 minute 
 int32 
 时间间隔分钟的整数 
 
 
 second 
 int32 
 时间间隔秒的整数 
 
 
 millisecond 
 int32 
 时间间隔毫秒的整数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
ts=TimeSpan(4,1,4,2,20);
string s3=ts.toString(); // s3="4.1:4:2.20"
 
 // 错误示例
TimeSpan ts;
ts=TimeSpan("4","1",4,2,20); // 编译失败
 

# 1.6.2 TimeSpanParse()

**TimeSpan >TimeSpanParse(string str)**
将字符串时间间隔转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
str
string
时间间隔字符串，格式规范:[-][d.]h:m[:s[.ff]] 或 [-][dd.]hh:mm[:ss[.ff]] （方括号中的元素可选）元素说明：
-: 可选负号，指示负的timespan
d: 天，范围介于0到10675199之间
h: 小时，范围介于0到23之间
m: 分钟，范围介于0到59之间
s: 秒，范围介于0到59之间
ff: 可选的秒的小数部分，由3到7个十进制数字组成。
参数
返回值
合法字符串
timespan类型
字符串不合法
0.0:0:0.000
**脚本示例**
// 示例1
string str="23:59";
TimeSpan ts;
ts =TimeSpanParse(str); 
string s=ts.toString(); // s="0.23:59:0.000"
// 示例2
string str="10.23:59:59.0";
TimeSpan ts;
ts =TimeSpanParse(str); 
string s=ts.toString();
Trace(2, "s=", s); // s="10.23:59:59.000"
// 错误示例
double str=23.32;
TimeSpan ts;
ts =TimeSpanParse(str); // 编译失败，参数类型错误

### 1.6.2 TimeSpanParse()

 TimeSpan >TimeSpanParse(string str) 

 
 
 
 功能描述 
 
 
 
 将字符串时间间隔转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 str 
 string 
 时间间隔字符串，格式规范:[-][d.]h:m[:s[.ff]] 或 [-][dd.]hh:mm[:ss[.ff]] （方括号中的元素可选）元素说明：
-: 可选负号，指示负的timespan
d: 天，范围介于0到10675199之间
h: 小时，范围介于0到23之间
m: 分钟，范围介于0到59之间
s: 秒，范围介于0到59之间
ff: 可选的秒的小数部分，由3到7个十进制数字组成。 
 
 
 
 
 
 返回值 
 
 
 
 
 参数 
 返回值 
 
 
 合法字符串 
 timespan类型 
 
 
 字符串不合法 
 0.0:0:0.000 
 
 
 脚本示例 

 // 示例1
string str="23:59";
TimeSpan ts;
ts =TimeSpanParse(str); 
string s=ts.toString(); // s="0.23:59:0.000"
 
 // 示例2
string str="10.23:59:59.0";
TimeSpan ts;
ts =TimeSpanParse(str); 
string s=ts.toString();
Trace(2, "s=", s); // s="10.23:59:59.000"
 
 // 错误示例
double str=23.32;
TimeSpan ts;
ts =TimeSpanParse(str); // 编译失败，参数类型错误
 

# 1.6.3 TimeSpanByTicks()

**TimeSpan >TimeSpanByTicks(int64 Ticks)**
将ticks转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
ticks
int64
100ns数
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
ts=TimeSpanByTicks(14789655321352);
string s =ts.toString(); // s="17.2:49:25.532"
// 示例2
TimeSpan ts;
ts=TimeSpanByTicks(10000);
string s =ts.toString(); // s="0.0:0:0.001"
// 示例3
TimeSpan ts;
ts=TimeSpanByTicks(-10000);
string s =ts.toString(); // s="-0.0:0:0.001"
// 错误示例
TimeSpan t;
string b="12342222";
t=TimeSpanByTicks(b);
string s =t.toString(); // 编译失败

### 1.6.3 TimeSpanByTicks()

 TimeSpan >TimeSpanByTicks(int64 Ticks) 

 
 
 
 功能描述 
 
 
 
 将ticks转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 ticks 
 int64 
 100ns数 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
ts=TimeSpanByTicks(14789655321352);
string s =ts.toString(); // s="17.2:49:25.532"
 
 // 示例2
TimeSpan ts;
ts=TimeSpanByTicks(10000);
string s =ts.toString(); // s="0.0:0:0.001"
 
 // 示例3
TimeSpan ts;
ts=TimeSpanByTicks(-10000);
string s =ts.toString(); // s="-0.0:0:0.001"
 
 // 错误示例
TimeSpan t;
string b="12342222";
t=TimeSpanByTicks(b);
string s =t.toString(); // 编译失败
 

# 1.6.4 TimeSpanByTotalDays()

**TimeSpan >TimeSpanByTotalDays(double totalDays)**
将总天数值转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
totalDays
double
总天数值
返回值
说明
timespan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
double day=1.1;
ts =TimeSpanByTotalDays(day);
string s =ts.toString(); // s="1.2:24:0.000"

### 1.6.4 TimeSpanByTotalDays()

 TimeSpan >TimeSpanByTotalDays(double totalDays) 

 
 
 
 功能描述 
 
 
 
 将总天数值转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 totalDays 
 double 
 总天数值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 timespan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
double day=1.1;
ts =TimeSpanByTotalDays(day);
string s =ts.toString(); // s="1.2:24:0.000"
 

# 1.6.5 TimeSpanByTotalHours()

**TimeSpan >TimeSpanByTotalHours(double totalHours)**
将总小时数值转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
totalHours
double
总小时数值
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
double hour=23;
ts =TimeSpanByTotalHours(hour);
string s =ts.toString(); // s="0.23:0:0.000"

### 1.6.5 TimeSpanByTotalHours()

 TimeSpan >TimeSpanByTotalHours(double totalHours) 

 
 
 
 功能描述 
 
 
 
 将总小时数值转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 totalHours 
 double 
 总小时数值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
double hour=23;
ts =TimeSpanByTotalHours(hour);
string s =ts.toString(); // s="0.23:0:0.000"
 

# 1.6.6 TimeSpanByTotalMinutes()

**TimeSpan >TimeSpanByTotalMinutes(double totalMinutes)**
将总分钟数值转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
totalMinutes
double
总分钟数值
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
double minu=40;
ts =TimeSpanByTotalMinutes(minu);
string s =ts.toString(); // s="0.0:40:0.0"

### 1.6.6 TimeSpanByTotalMinutes()

 TimeSpan >TimeSpanByTotalMinutes(double totalMinutes) 

 
 
 
 功能描述 
 
 
 
 将总分钟数值转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 totalMinutes 
 double 
 总分钟数值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
double minu=40;
ts =TimeSpanByTotalMinutes(minu);
string s =ts.toString(); // s="0.0:40:0.0"
 

# 1.6.7 TimeSpanByTotalSeconds()

**TimeSpan >TimeSpanByTotalSeconds(double totalSeconds)**
将总秒数值转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
totalSeconds
double
总秒数值
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
double sec=40;
ts =TimeSpanByTotalSeconds(sec);
string s =ts.toString(); // s="0.0:0:40.0"

### 1.6.7 TimeSpanByTotalSeconds()

 TimeSpan >TimeSpanByTotalSeconds(double totalSeconds) 

 
 
 
 功能描述 
 
 
 
 将总秒数值转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 totalSeconds 
 double 
 总秒数值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
double sec=40;
ts =TimeSpanByTotalSeconds(sec);
string s =ts.toString(); // s="0.0:0:40.0"
 

# 1.6.8 TimeSpanByTotalMilliseconds()

**TimeSpan >TimeSpanByTotalMilliseconds(double totalMilliseconds)**
将总毫秒数值转为TimeSpan，参数可为负，表示负的时间间隔。
参数名
类型
说明
totalMilliseconds
double
总毫秒数值
返回值
说明
TimeSpan类型
时间间隔类型值
**脚本示例**
// 示例1
TimeSpan ts;
double msec=400;
ts =TimeSpanByTotalMilliseconds(msec);
string s =ts.toString(); // s="0.0:0:0.400"

### 1.6.8 TimeSpanByTotalMilliseconds()

 TimeSpan >TimeSpanByTotalMilliseconds(double totalMilliseconds) 

 
 
 
 功能描述 
 
 
 
 将总毫秒数值转为TimeSpan，参数可为负，表示负的时间间隔。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 totalMilliseconds 
 double 
 总毫秒数值 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔类型值 
 
 
 脚本示例 

 // 示例1
TimeSpan ts;
double msec=400;
ts =TimeSpanByTotalMilliseconds(msec);
string s =ts.toString(); // s="0.0:0:0.400"
 

# 1.6.9 TimeDiff()

**TimeSpan >TimeDiff(datetime dt1,datetime dt2)**
获取两个DateTime的时间差。
参数名
类型
说明
dt1
datetime
时间1
dt2
datetime
时间2
返回值
说明
TimeSpan类型
时间间隔=dt1-dt2
**脚本示例**
DateTime time1=DateTime(2021,5,8,12,15,20,25,0);
DateTime time2=DateTime(2020,5,8,12,15,20,25,0);
TimeSpan ts=TimeDiff(time1,time2);
string s=ts.toString();
Trace(2, "s:", s); // s:365.0:0:0.000

### 1.6.9 TimeDiff()

 TimeSpan >TimeDiff(datetime dt1,datetime dt2) 

 
 
 
 功能描述 
 
 
 
 获取两个DateTime的时间差。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 dt1 
 datetime 
 时间1 
 
 
 dt2 
 datetime 
 时间2 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 TimeSpan类型 
 时间间隔=dt1-dt2 
 
 
 脚本示例 

 DateTime time1=DateTime(2021,5,8,12,15,20,25,0);
DateTime time2=DateTime(2020,5,8,12,15,20,25,0);
TimeSpan ts=TimeDiff(time1,time2);
string s=ts.toString();
Trace(2, "s:", s); // s:365.0:0:0.000
 

# 1.7 Trace函数

## 1.7 Trace函数

# 1.7.1 Trace()

**void >Trace(int32 logLevel,string description,var content)；**
按级别输出脚本日志。
**规则约束**
1、能输出var支持的所有数据类型。
2、当日志级别 void >Trace(int32 logLevel,string description,var content)； 

 
 
 
 功能描述 
 
 
 
 按级别输出脚本日志。 
 
 
 规则约束 
 
 
 1、能输出var支持的所有数据类型。 
 
 
 2、当日志级别<=App对象设置的日志输出级别（默认为Level4），则输出。如：App对象1设置为Level2级别，则对应输出Level1和Level2脚本日志内容。（可通过桌面工具设置）。 
 
 
 3、每个App对象脚本日志输出形式：单行输出、多行输出，默认多行输出。（可通过桌面工具设置）。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 logLevel 
 int32 
 日志级别：1~5 
 
 
 description 
 string 
 描述信息 
 
 
 content 
 var 
 输出日志内容 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 无返回值 
 
 
 
 脚本示例 

 string str = "abcdefg";
Trace(2, "输出的字符串:", str); // 脚本日志显示：输出的字符串:abcdefg
 

# 1.8 加解密函数

## 1.8 加解密函数

# 1.8.1 Rsa加解密函数

### 1.8.1 Rsa加解密函数

# 1.8.1.1 GenRsaKey()

**string[] >GenRsaKey(int32 bits)**
根据密钥长度生成RSA密钥对。
参数名
类型
说明
bits
int32
RSA密钥长度，支持512bit、1024bit、2048bit、4096bit，不填或填写其他值默认按512bit处理
返回值
说明
string[1]
返回RSA公钥
string[2]
返回RSA私钥
**脚本示例**
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];

#### 1.8.1.1 GenRsaKey()

 string[] >GenRsaKey(int32 bits) 

 
 
 
 功能描述 
 
 
 
 根据密钥长度生成RSA密钥对。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 bits 
 int32 
 RSA密钥长度，支持512bit、1024bit、2048bit、4096bit，不填或填写其他值默认按512bit处理 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string[1] 
 返回RSA公钥 
 
 
 string[2] 
 返回RSA私钥 
 
 
 脚本示例 

 int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];
 

# 1.8.1.2 RsaEncrypt()

**blob >RsaEncrypt(string key,bool keyType,blob originalData)**
根据RSA密钥对数据加密。一般是通过公钥加密，私钥解密，也可以通过私钥加密，公钥解密
参数名
类型
说明
key
string
RSA密钥,可以是公钥pubkey或私钥prvkey，一般是公钥；可省略，省略时使用WOS系统维护的密钥（使用WOS系统维护的密钥暂未支持）
keyType
bool
0:公钥：1：私钥，默认为0
originalData
blob
原数据，即要加密的数据
返回值
说明
encryptedData
加密后的数据
**脚本示例**
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];

string text="一切安好";
blob blobData;
blobData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,blobData);

#### 1.8.1.2 RsaEncrypt()

 blob >RsaEncrypt(string key,bool keyType,blob originalData) 

 
 
 
 功能描述 
 
 
 
 根据RSA密钥对数据加密。一般是通过公钥加密，私钥解密，也可以通过私钥加密，公钥解密 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 RSA密钥,可以是公钥pubkey或私钥prvkey，一般是公钥；可省略，省略时使用WOS系统维护的密钥（使用WOS系统维护的密钥暂未支持） 
 
 
 keyType 
 bool 
 0:公钥：1：私钥，默认为0 
 
 
 originalData 
 blob 
 原数据，即要加密的数据 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 encryptedData 
 加密后的数据 
 
 
 脚本示例 

 int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];

string text="一切安好";
blob blobData;
blobData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,blobData);
 

# 1.8.1.3 RsaDecrypt()

**blob >RsaDecrypt(string key,bool keyType,blob encryptedData)**
根据RSA密钥对数据解密。一般是通过公钥加密，私钥解密，也可以通过私钥加密，公钥解密
参数名
类型
说明
key
string
RSA密钥,可以是公钥pubkey或私钥prikey，一般是私钥；可省略，省略时使用WOS系统维护的密钥（使用WOS系统维护的密钥暂未支持）
keyType
bool
0:公钥：1：私钥，默认为0
encryptedData
blob
加密后的数据
返回值
说明
originalData
解密后的数据
**脚本示例**
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];

string text="一切安好";
blob blobData;
blobData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,blobData);

blob originalData;
originalData=RsaDecrypt(prikey,1,encryptedData);

#### 1.8.1.3 RsaDecrypt()

 blob >RsaDecrypt(string key,bool keyType,blob encryptedData) 

 
 
 
 功能描述 
 
 
 
 根据RSA密钥对数据解密。一般是通过公钥加密，私钥解密，也可以通过私钥加密，公钥解密 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 RSA密钥,可以是公钥pubkey或私钥prikey，一般是私钥；可省略，省略时使用WOS系统维护的密钥（使用WOS系统维护的密钥暂未支持） 
 
 
 keyType 
 bool 
 0:公钥：1：私钥，默认为0 
 
 
 encryptedData 
 blob 
 加密后的数据 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 originalData 
 解密后的数据 
 
 
 脚本示例 

 int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];

string text="一切安好";
blob blobData;
blobData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,blobData);

blob originalData;
originalData=RsaDecrypt(prikey,1,encryptedData);
 

# 1.8.1.4 RsaSign()

**blob >RsaSign(string key,bool keyType,blob originalData)**
生成数字签名。
使用哈希函数（SHA-256）对原始信息进行摘要计算，得到数字摘要，再通过密钥（私钥）对数字摘要进行加密，生成数字签名。
参数名
类型
说明
key
string
RSA密钥,可以是公钥pubkey或私钥prikey，一般是私钥；可省略，省略时使用WOS系统维护的密钥
keyType
bool
0:公钥：1：私钥，默认为0
originalData
blob
原始信息，即要加签的数据。
返回值
说明
signedData
数字签名
**脚本示例**
string text="这是一段信息";
blob originalData;
originalData.appendString(text);
blob signedData;
signedData=RsaSign("",1,originalData);//key可省略，省略时使用WOS系统维护的密钥，使用WOS系统维护的密钥暂未支持

#### 1.8.1.4 RsaSign()

 blob >RsaSign(string key,bool keyType,blob originalData) 

 
 
 
 功能描述 
 
 
 
 生成数字签名。
使用哈希函数（SHA-256）对原始信息进行摘要计算，得到数字摘要，再通过密钥（私钥）对数字摘要进行加密，生成数字签名。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 RSA密钥,可以是公钥pubkey或私钥prikey，一般是私钥；可省略，省略时使用WOS系统维护的密钥 
 
 
 keyType 
 bool 
 0:公钥：1：私钥，默认为0 
 
 
 originalData 
 blob 
 原始信息，即要加签的数据。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 signedData 
 数字签名 
 
 
 脚本示例 

 string text="这是一段信息";
blob originalData;
originalData.appendString(text);
blob signedData;
signedData=RsaSign("",1,originalData);//key可省略，省略时使用WOS系统维护的密钥，使用WOS系统维护的密钥暂未支持
 

# 1.8.1.5 RsaVerifySign()

**bool >RsaVerifySign(string key,bool keyType,blob originalData,blob signedData)**
验证数字签名是否有效。
使用哈希函数（SHA-256）对原始信息进行摘要计算，得到数字摘要，再通过密钥（公钥）对数字签名进行解密得到另一个数字摘要，两个数字摘要进行对比，两者相同则说明信息没有被篡改，数字签名有效，反之无效。
参数名
类型
说明
key
string
RSA密钥,可以是公钥pubkey或私钥prikey，一般是公钥；可省略，省略时使用WOS系统维护的密钥
keyType
bool
0:公钥：1：私钥，默认为0
originalData
blob
原始信息
signedData
blob
数字签名
返回值
说明
0
数字签名无效
1
数字签名有效
**脚本示例**
//生成数字签名
string text="这是一段信息";
blob originalData;
originalData.appendString(text);
blob signedData;
signedData=RsaSign("",0,originalData);

//验证数字签名是否有效
bool isValid=RsaVerifySign("",0,originalData,signedData);//key可省略，省略时使用WOS系统维护的密钥，使用WOS系统维护的密钥暂未支持
if(isValid==true)
{
 TraceString("数字签名:","有效"); 
}else{
 TraceString("数字签名:","无效"); 
}
//综合脚本示例
//根据密钥长度生成RSA密钥对
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];
TraceString("pubkey=",pubkey);
TraceString("prikey=",prikey);

//根据RSA密钥对数据加密,公钥加密
string text="一切安好";
blob originalData;
originalData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,originalData);

//根据RSA密钥对数据解密，私钥解密
blob originalData1;
originalData1=RsaDecrypt(prikey,1,encryptedData);

string text1=originalData1.toString();
TraceString("text1=",text1);
if(text==text1)
{
 TraceString("解密验证结果=","通过"); 
}else{
 TraceString("解密验证结果=","不通过"); 
}

//生成数字签名,私钥加密
string text2="这是一段信息";
blob originalData2;
originalData2.appendString(text2);
blob signedData2;
signedData2=RsaSign(prikey,1,originalData2);

//验证数字签名是否有效
bool isValid=RsaVerifySign(pubkey,0,originalData2,signedData2);
if(isValid==true)
{
 TraceString("数字签名验证结果=","有效"); 
}else{
 TraceString("数字签名验证结果=:","无效"); 
}

#### 1.8.1.5 RsaVerifySign()

 bool >RsaVerifySign(string key,bool keyType,blob originalData,blob signedData) 

 
 
 
 功能描述 
 
 
 
 验证数字签名是否有效。
使用哈希函数（SHA-256）对原始信息进行摘要计算，得到数字摘要，再通过密钥（公钥）对数字签名进行解密得到另一个数字摘要，两个数字摘要进行对比，两者相同则说明信息没有被篡改，数字签名有效，反之无效。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 RSA密钥,可以是公钥pubkey或私钥prikey，一般是公钥；可省略，省略时使用WOS系统维护的密钥 
 
 
 keyType 
 bool 
 0:公钥：1：私钥，默认为0 
 
 
 originalData 
 blob 
 原始信息 
 
 
 signedData 
 blob 
 数字签名 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 0 
 数字签名无效 
 
 
 1 
 数字签名有效 
 
 
 脚本示例 

 //生成数字签名
string text="这是一段信息";
blob originalData;
originalData.appendString(text);
blob signedData;
signedData=RsaSign("",0,originalData);

//验证数字签名是否有效
bool isValid=RsaVerifySign("",0,originalData,signedData);//key可省略，省略时使用WOS系统维护的密钥，使用WOS系统维护的密钥暂未支持
if(isValid==true)
{
 TraceString("数字签名:","有效"); 
}else{
 TraceString("数字签名:","无效"); 
}
 
 //综合脚本示例
//根据密钥长度生成RSA密钥对
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];
TraceString("pubkey=",pubkey);
TraceString("prikey=",prikey);

//根据RSA密钥对数据加密,公钥加密
string text="一切安好";
blob originalData;
originalData.appendString(text);;
blob encryptedData;
encryptedData=RsaEncrypt(pubkey,0,originalData);

//根据RSA密钥对数据解密，私钥解密
blob originalData1;
originalData1=RsaDecrypt(prikey,1,encryptedData);

string text1=originalData1.toString();
TraceString("text1=",text1);
if(text==text1)
{
 TraceString("解密验证结果=","通过"); 
}else{
 TraceString("解密验证结果=","不通过"); 
}

//生成数字签名,私钥加密
string text2="这是一段信息";
blob originalData2;
originalData2.appendString(text2);
blob signedData2;
signedData2=RsaSign(prikey,1,originalData2);

//验证数字签名是否有效
bool isValid=RsaVerifySign(pubkey,0,originalData2,signedData2);
if(isValid==true)
{
 TraceString("数字签名验证结果=","有效"); 
}else{
 TraceString("数字签名验证结果=:","无效"); 
}
 

# 1.8.2 Aes加解密函数

### 1.8.2 Aes加解密函数

# 1.8.2.1 GenAesKey()

**string >GenAesKey(int32 keySize，int32 keyCode)**
随机生成指定长度的密钥。
参数名
类型
说明
keySize
int32
密钥key长度，0：128位，1：192位，2：256位，填其他值或不赋值按0处理
keyCode
int32
密钥key编码格式，0：utf-8，1：base64，2：hex，填其他值或不赋值按0处理
返回值
说明
string
随机生成的指定长度的密钥
**脚本示例**
int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);

#### 1.8.2.1 GenAesKey()

 string >GenAesKey(int32 keySize，int32 keyCode) 

 
 
 
 功能描述 
 
 
 
 随机生成指定长度的密钥。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 keySize 
 int32 
 密钥key长度，0：128位，1：192位，2：256位，填其他值或不赋值按0处理 
 
 
 keyCode 
 int32 
 密钥key编码格式，0：utf-8，1：base64，2：hex，填其他值或不赋值按0处理 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 随机生成的指定长度的密钥 
 
 
 脚本示例 

 int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);
 

# 1.8.2.2 GetRandomBytes()

**string >GetRandomBytes(int32 ivSize，int32 ivCode)**
生成随机初始向量（iv）
参数名
类型
说明
ivSize
int32
初始向量iv长度，0：128位，1：96位，填其他值或不赋值按0处理
ivcode
int32
初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其他值或不赋值按0处理
返回值
说明
string
生成的随机初始向量iv
**脚本示例**
int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);

#### 1.8.2.2 GetRandomBytes()

 string >GetRandomBytes(int32 ivSize，int32 ivCode) 

 
 
 
 功能描述 
 
 
 
 生成随机初始向量（iv） 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 ivSize 
 int32 
 初始向量iv长度，0：128位，1：96位，填其他值或不赋值按0处理 
 
 
 ivcode 
 int32 
 初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其他值或不赋值按0处理 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 生成的随机初始向量iv 
 
 
 脚本示例 

 int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);
 

# 1.8.2.3 AesEncrypt();

**blob>AesEncrypt(string key, string iv, Blob originalData, int32 aesMode, int32 keyCode, int32 ivCode, int32 paddingType)**
AES加密，每次加密时请使用不同的IV，保证安全性。
参数名
类型
说明
key
string
密钥key
iv
string
初始向量iv
originalData
blob
原数据，即要加密的数据
aesMode
int32
加密模式，0：CBC，1：GCM，填其它值或不赋值默认按0：CBC处理
keyCode
int32
密钥key编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理
ivCode
int32
初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理
paddingType
int32
填充方式；0：PKCS#5，1：PKCS#7，2：Zeros; 暂不支持其它填充方式，填其它值或不赋值按0处理
返回值
说明
blob
AES加密后的数据
**脚本示例**
//生成密钥
int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);
// 生成iv
int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);
//AES加密
blob originalData;
string str ="这是一段文字";
originalData.appendString(str);
int32 aesMode = 0;
int32 paddingType = 0;
Blob encryptedData = AesEncrypt(key, iv, originalData, aesMode, keyCode, ivCode, paddingType);

#### 1.8.2.3 AesEncrypt();

 blob>AesEncrypt(string key, string iv, Blob originalData, int32 aesMode, int32 keyCode, int32 ivCode, int32 paddingType) 

 
 
 
 功能描述 
 
 
 
 AES加密，每次加密时请使用不同的IV，保证安全性。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 密钥key 
 
 
 iv 
 string 
 初始向量iv 
 
 
 originalData 
 blob 
 原数据，即要加密的数据 
 
 
 aesMode 
 int32 
 加密模式，0：CBC，1：GCM，填其它值或不赋值默认按0：CBC处理 
 
 
 keyCode 
 int32 
 密钥key编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理 
 
 
 ivCode 
 int32 
 初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理 
 
 
 paddingType 
 int32 
 填充方式；0：PKCS#5，1：PKCS#7，2：Zeros; 暂不支持其它填充方式，填其它值或不赋值按0处理 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 blob 
 AES加密后的数据 
 
 
 脚本示例 

 //生成密钥
int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);
// 生成iv
int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);
//AES加密
blob originalData;
string str ="这是一段文字";
originalData.appendString(str);
int32 aesMode = 0;
int32 paddingType = 0;
Blob encryptedData = AesEncrypt(key, iv, originalData, aesMode, keyCode, ivCode, paddingType);
 

# 1.8.2.4 AesDecrypt();

**blob>AesDecrypt(string key, string iv, Blob encryptedData, int32 aesMode, int32 keyCode, int32 ivCode);** 
AES解密，解密时需要使用加密时使用的key和IV。
参数名
类型
说明
key
string
密钥key
iv
string
初始向量iv
encryptedData
blob
AES加密后的数据
aesMode
int32
加密模式，0：CBC，1：GCM，填其它值或不赋值默认按0：CBC处理
keyCode
int32
密钥key编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理
ivCode
int32
初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理
paddingType
int32
填充方式；0：PKCS#5，1：PKCS#7，2：Zeros; 暂不支持其它填充方式，填其它值或不赋值按0处理
返回值
说明
blob
AES解密后的数据
**脚本示例**
//生成密钥
int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);
// 生成iv
int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);
//AES加密
blob originalData;
string str ="这是一段文字";
originalData.appendString(str);
int32 aesMode = 0;
int32 paddingType = 0;
Blob encryptedData = AesEncrypt(key, iv, originalData, aesMode, keyCode, ivCode, paddingType);
//AES解密
blob originalData2 = AesDecrypt(key, iv, encryptedData, aesMode, keyCode, ivCode, paddingType);
// 判断两个blob是否相等
bool comparisonResult = originalData.isEqual(originalData2); 
if(True == comparisonResult)
{
 TraceString("解密结果验证=","通过");
}
else
{
 TraceString("解密结果验证=","不通过");
}

#### 1.8.2.4 AesDecrypt();

 blob>AesDecrypt(string key, string iv, Blob encryptedData, int32 aesMode, int32 keyCode, int32 ivCode); 

 
 
 
 功能描述 
 
 
 
 AES解密，解密时需要使用加密时使用的key和IV。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 密钥key 
 
 
 iv 
 string 
 初始向量iv 
 
 
 encryptedData 
 blob 
 AES加密后的数据 
 
 
 aesMode 
 int32 
 加密模式，0：CBC，1：GCM，填其它值或不赋值默认按0：CBC处理 
 
 
 keyCode 
 int32 
 密钥key编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理 
 
 
 ivCode 
 int32 
 初始向量iv编码格式，0：utf-8，1：base64，2：hex，填其它值或不赋值按0处理 
 
 
 paddingType 
 int32 
 填充方式；0：PKCS#5，1：PKCS#7，2：Zeros; 暂不支持其它填充方式，填其它值或不赋值按0处理 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 blob 
 AES解密后的数据 
 
 
 脚本示例 

 //生成密钥
int32 keySize = 0;
int32 keyCode = 0;
string key;
key=GenAesKey(keySize, keyCode);
// 生成iv
int32 ivSize = 0;
int32 ivCode = 0;
string iv;
iv=GetRandomBytes(ivSize, ivCode);
//AES加密
blob originalData;
string str ="这是一段文字";
originalData.appendString(str);
int32 aesMode = 0;
int32 paddingType = 0;
Blob encryptedData = AesEncrypt(key, iv, originalData, aesMode, keyCode, ivCode, paddingType);
//AES解密
blob originalData2 = AesDecrypt(key, iv, encryptedData, aesMode, keyCode, ivCode, paddingType);
// 判断两个blob是否相等
bool comparisonResult = originalData.isEqual(originalData2); 
if(True == comparisonResult)
{
 TraceString("解密结果验证=","通过");
}
else
{
 TraceString("解密结果验证=","不通过");
}
 

# 1.9 其它函数

## 1.9 其它函数

# 1.9.1 Typeof()

**功能描述：**获取类型
**string >Typeof(var value)**
获取指定变量的数据类型，成功，则返回类型字符串(比如返回：“char”)，失败，则返回错误值。
参数名
类型
说明
value
var
不同类型的变量，支持的类型见附表1。
返回值
说明
string
变量类型
char
返回int8类型
bool

int

int8

uint8

int16

uint16

int32

uint32

int64

uint64

float

double

decimal

string

blob

array

datetime

timespan

struct
返回结构体名称
JSONObject

map

DataSet

var
返回var的具体类型
void
空类型，用于dataSet数据集；返回数据集后，使用typeof()函数进行null值判断，null值为void类型。
**脚本示例**
int64 a1=2;
string b1;
b1=Typeof(a1); // b1="int64"
// 对struct类型判断：
// 可判断用户自定义的结构体类型
// 不同类型的结构体 用var[]去接收（struct[]只能接收固定类型的结构体） 
struct A
{
 int number;
 int type;
};
struct B
{
 int c;
 int d;
};
////////////////////////////////////////////
A a1;
B b1;
var[] arry={a1,b1}; // var数组接收多个结构体
string s=Typeof(arry[1]); // 判断结构体类型为A 
if(s=="A")
{
A aa=arry[1]; // 如果用结构体B去接这个数据，编译会报错
int32 ww=aa.number;
}

### 1.9.1 Typeof()

 功能描述： 获取类型

 string >Typeof(var value) 

 
 
 
 功能描述 
 
 
 
 获取指定变量的数据类型，成功，则返回类型字符串(比如返回：“char”)，失败，则返回错误值。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 value 
 var 
 不同类型的变量，支持的类型见附表1。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 变量类型 
 
 
 
 
 
 变量类型 
 说明 
 
 
 
 char 
 返回int8类型 
 
 
 bool 
 
 
 
 int 
 
 
 
 int8 
 
 
 
 uint8 
 
 
 
 int16 
 
 
 
 uint16 
 
 
 
 int32 
 
 
 
 uint32 
 
 
 
 int64 
 
 
 
 uint64 
 
 
 
 float 
 
 
 
 double 
 
 
 
 decimal 
 
 
 
 string 
 
 
 
 blob 
 
 
 
 array 
 
 
 
 datetime 
 
 
 
 timespan 
 
 
 
 struct 
 返回结构体名称 
 
 
 JSONObject 
 
 
 
 map 
 
 
 
 DataSet 
 
 
 
 var 
 返回var的具体类型 
 
 
 void 
 空类型，用于dataSet数据集；返回数据集后，使用typeof()函数进行null值判断，null值为void类型。 
 
 
 脚本示例 

 int64 a1=2;
string b1;
b1=Typeof(a1); // b1="int64"
 
 // 对struct类型判断：
// 可判断用户自定义的结构体类型
// 不同类型的结构体 用var[]去接收（struct[]只能接收固定类型的结构体） 
struct A
{
 int number;
 int type;
};
struct B
{
 int c;
 int d;
};
////////////////////////////////////////////
A a1;
B b1;
var[] arry={a1,b1}; // var数组接收多个结构体
string s=Typeof(arry[1]); // 判断结构体类型为A 
if(s=="A")
{
A aa=arry[1]; // 如果用结构体B去接这个数据，编译会报错
int32 ww=aa.number;
}
 

# 1.9.2 IsEqual()

**bool >IsEqual(double data1,double data2,double EPSINON)**
在精度范围内，判断两个浮点数是否相等
浮点数（float、double）的比较问题，由于浮点数存在精度损失问题，不可直接使用"=="或"！="与任何数字比较，建议转化为">="或" =-EPSINON) && (a =10^-6次方，如果小于10^-6次方，会按10^-6次方处理。
返回值
说明
0
不等
1
相等
**脚本示例**
double a=1.12345;
double b=1.12343;
double c1=0.00001;
double c2=0.00002;
bool d1=IsEqual(a,b,c1); // d1=0
bool d2=IsEqual(a,b,c2); // d2=1

### 1.9.2 IsEqual()

 bool >IsEqual(double data1,double data2,double EPSINON) 

 
 
 
 功能描述 
 
 
 
 在精度范围内，判断两个浮点数是否相等 
 
 
 浮点数（float、double）的比较问题，由于浮点数存在精度损失问题，不可直接使用"=="或"！="与任何数字比较，建议转化为">="或"<="形式，比如：
double a;
If (a==0.0) //隐含错误的比较
改为：
If (a>=-EPSINON) && (a<=EPSINON))
EPSINON代表允许的误差，转为判断是不是落在范围内。
或者使用比较函数：
bool isEqual(double data1,double data2,double EPSINON) 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 data1 
 double 
 要比较的浮点数1 
 
 
 data2 
 double 
 要比较的浮点数2 
 
 
 EPSINON 
 double 
 精度，允许的误差。精度如果过小，浮点数计算，导致精度丢失会使结果不准确。
限制EPSINON>=10^-6次方，如果小于10^-6次方，会按10^-6次方处理。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 0 
 不等 
 
 
 1 
 相等 
 
 
 脚本示例 

 double a=1.12345;
double b=1.12343;
double c1=0.00001;
double c2=0.00002;
bool d1=IsEqual(a,b,c1); // d1=0
bool d2=IsEqual(a,b,c2); // d2=1
 

# 1.9.3 Max()

**uint64 >Max(uint64 data1, uint64 data2)**
**int64 >Max(int64 data1, int64 data2)**
**double >Max(double data1,double data2,double EPSINON)**
求UInt64、Int64类型的最大值。
在精度范围内，求Double类型的最大值，如果按精度比较，2个数是一样的，就返回第一个数。
输入参数如果是字符串类型，返回编译错误，如果是var类型，运行时，如果var里是字符串，则转成数值，进行比较。
参数名
类型
说明
data1
uint64、int64、double
要比较的数1
data2
uint64、int64、double
要比较的数2
EPSINON
double
浮点数比较的精度，允许的误差。精度参数可缺省，精度缺省则默认为：0.000001。
精度如果过小，浮点数计算，导致精度丢失会使结果不准确。
限制EPSINON>=10^-6次方，如果小于10^-6次方，会按10^-6次方处理。
返回值
说明
uint64、int64、double
返回最大值
**脚本示例**
double a1 =Max(1999.111, 1999.112); // a1=1999.112
double a2 =Max(1999.111, 1999.111); // a2=1999.111

### 1.9.3 Max()

 uint64 >Max(uint64 data1, uint64 data2) 

 int64 >Max(int64 data1, int64 data2) 

 double >Max(double data1,double data2,double EPSINON) 

 
 
 
 功能描述 
 
 
 
 求UInt64、Int64类型的最大值。 
 
 
 在精度范围内，求Double类型的最大值，如果按精度比较，2个数是一样的，就返回第一个数。 
 
 
 输入参数如果是字符串类型，返回编译错误，如果是var类型，运行时，如果var里是字符串，则转成数值，进行比较。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 data1 
 uint64、int64、double 
 要比较的数1 
 
 
 data2 
 uint64、int64、double 
 要比较的数2 
 
 
 EPSINON 
 double 
 浮点数比较的精度，允许的误差。精度参数可缺省，精度缺省则默认为：0.000001。
精度如果过小，浮点数计算，导致精度丢失会使结果不准确。
限制EPSINON>=10^-6次方，如果小于10^-6次方，会按10^-6次方处理。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 uint64、int64、double 
 返回最大值 
 
 
 脚本示例 

 double a1 =Max(1999.111, 1999.112); // a1=1999.112
double a2 =Max(1999.111, 1999.111); // a2=1999.111
 

# 1.9.4 Min()

**uint64 >Min(uint64 data1, uint64 data2)**
**int64 >Min(int64 data1, int64 data2)**
**double >Min(double data1,double data2,double EPSINON)**
求uInt64、int64类型的最小值。
在精度范围内，求double类型的最小值，如果按精度比较，2个数是一样的，就返回第一个数。
输入参数如果是字符串类型，返回编译错误，如果是var类型，运行时，如果var里是字符串，则转成数值，进行比较。
参数名
类型
说明
data1
uint64、int64、double
要比较的数1
data2
uint64、int64、double
要比较的数2
EPSINON
double
浮点数比较的精度，允许的误差。精度参数可缺省，精度缺省则默认为：0.000001。
精度如果过小，浮点数计算，导致精度丢失会使结果不准确。
限制EPSINON>=10^-6次方，如果小于10^-6次方，会按10^-6次方处理。
返回值
说明
uint64、int64、double
返回最小值
**脚本示例**
double b1 =Min(1999.111, 1999.112); // b=1999.111
double b2 =Min(1999.111, 1999.111); // b=1999.111

### 1.9.4 Min()

 uint64 >Min(uint64 data1, uint64 data2) 

 int64 >Min(int64 data1, int64 data2) 

 double >Min(double data1,double data2,double EPSINON) 

 
 
 
 功能描述 
 
 
 
 求uInt64、int64类型的最小值。 
 
 
 在精度范围内，求double类型的最小值，如果按精度比较，2个数是一样的，就返回第一个数。 
 
 
 输入参数如果是字符串类型，返回编译错误，如果是var类型，运行时，如果var里是字符串，则转成数值，进行比较。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 data1 
 uint64、int64、double 
 要比较的数1 
 
 
 data2 
 uint64、int64、double 
 要比较的数2 
 
 
 EPSINON 
 double 
 浮点数比较的精度，允许的误差。精度参数可缺省，精度缺省则默认为：0.000001。
精度如果过小，浮点数计算，导致精度丢失会使结果不准确。
限制EPSINON>=10^-6次方，如果小于10^-6次方，会按10^-6次方处理。 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 uint64、int64、double 
 返回最小值 
 
 
 脚本示例 

 double b1 =Min(1999.111, 1999.112); // b=1999.111
double b2 =Min(1999.111, 1999.111); // b=1999.111
 

# 1.9.5 Sleep()

**void >Sleep(int64 time)**
停止该线程上的所有App执行元语言脚本，等待一段时间再继续执行。
参数名
类型
说明
time
int64
等待时间，毫秒为单位；
数值小与0时，按照0处理（不等待）
返回值
说明
无

**脚本示例**
datetime currentTime1 = Now();
string res1=currentTime1.toString("Fms"); // res1="2021-3-4 13:48:34.67"
Sleep(2000);
datetime currentTime2 = Now();
string res2=currentTime2.toString("Fms"); // res2="2021-3-4 13:48:36.67"

### 1.9.5 Sleep()

 void >Sleep(int64 time) 

 
 
 
 功能描述 
 
 
 
 停止该线程上的所有App执行元语言脚本，等待一段时间再继续执行。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 time 
 int64 
 等待时间，毫秒为单位；
数值小与0时，按照0处理（不等待） 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 无 
 
 
 
 脚本示例 

 datetime currentTime1 = Now();
string res1=currentTime1.toString("Fms"); // res1="2021-3-4 13:48:34.67"
Sleep(2000);
datetime currentTime2 = Now();
string res2=currentTime2.toString("Fms"); // res2="2021-3-4 13:48:36.67"
 

# 1.9.6 GetErrorMessage()

**string >GetErrorMessage(Int32 errCode,int8 lang)**
获取错误码描述
**规则约束**
enum LanguageCodeEnum
{
LAN_CODE_Local = -1, //获取操作系统语言类型
LAN_CODE_ZH_CN = 0, // default: Simplified Chinese
LAN_CODE_EN_US=1, // English US
LAN_CODE_JA_JP=2, // Japanese
LAN_CODE_KO_KR, // Korean
LAN_CODE_RU_RU, // Russian
LAN_CODE_ZH_HK, // Traditional Chinese(HongKong)
LAN_CODE_ZH_MO, // Traditional Chinese(Macao)
LAN_CODE_ZH_TW, // Traditional Chinese(Taiwan)
};
参数名
类型
说明
errCode
Int32
错误码
lang
int8
语言类型
返回值
说明

描述，如果按语言类型查不到描述，则返回空
**脚本示例**
// 框架及公共模块错误码
string errorMessage1;
errorMessage1 = GetErrorMessage(536903682, 0);
Trace(2, "框架及公共模块错误码=", errorMessage1); // 框架及公共模块错误码=致命错误
// 模型库错误码
string errorMessage2;
errorMessage2 = GetErrorMessage(-10111, 0);
Trace(2, "模型库错误码=", errorMessage2); // 模型库错误码=数据区ID数据类型不匹配
// 实时库错误码
string errorMessage3;
errorMessage3 = GetErrorMessage(-20052, 0);
Trace(2, "实时库错误码=", errorMessage3); // 实时库错误码=input内函数参数的类型转换失败
// 历史库&计划库错误码
string errorMessage4;
errorMessage4 = GetErrorMessage(-30001, 0);
Trace(2, "历史库&计划库错误码=", errorMessage4); // 历史库&计划库错误码=cloudID超出允许范围
// 图关系库错误码
string errorMessage5;
errorMessage5 = GetErrorMessage(-50000, 0);
Trace(2, "图关系库错误码=", errorMessage5); // 图关系库错误码=批量接口返回部分成功/部分失败
// 空间库错误码
string errorMessage6;
errorMessage6 = GetErrorMessage(906035205, 0);
Trace(2, "空间库错误码=", errorMessage6); // 空间库错误码=获取服务失败
// 接口库错误码
string errorMessage7;
errorMessage7 = GetErrorMessage(-70100, 0);
Trace(2, "接口库错误码=", errorMessage7); // 接口库错误码=接口库_TCP服务端错误
// 资源库错误码
string errorMessage8;
errorMessage8 = GetErrorMessage(-80260, 0);
Trace(2, "资源库错误码=", errorMessage8); // 资源库错误码=获取服务失败
// 视频库错误码
string errorMessage9;
errorMessage9 = GetErrorMessage(-92102, 0);
Trace(2, "视频库错误码getErrorMessage9=", errorMessage9); // 视频库错误码getErrorMessage9=参数类型错误
// 安全库错误码
string errorMessage10;
errorMessage10 = GetErrorMessage(-100003, 0);
Trace(2, "安全库错误码=", errorMessage10); // 安全库错误码=参数类型错误
// 审计库错误码
string errorMessage11;
errorMessage11 = GetErrorMessage(-120002, 0);
Trace(2, "审计库错误码=", errorMessage11); // 审计库错误码=审计库链接异常
// 运维错误码
string errorMessage12;
errorMessage12 = GetErrorMessage(-140003, 0);
Trace(2, "运维错误码=", errorMessage12); // 运维错误码=未找到仓库
// 网关错误码
string errorMessage13;
errorMessage13 = GetErrorMessage(-180999, 0);
Trace(2, "网关错误码=", errorMessage13); // 网关错误码=请求参数无效
// 功能单元库错误码
string errorMessage14;
errorMessage14 = GetErrorMessage(-210069, 0);
Trace(2, "功能单元库错误码=", errorMessage14); // 功能单元库错误码=ID数量与模型版本数量不一致
// 图形库错误码
string errorMessage15;
errorMessage15 = GetErrorMessage(-349999, 0);
Trace(2, "图形库错误码=", errorMessage15); // 图形库错误码=缺失type
// 事务协调器错误码
string errorMessage16;
errorMessage16 = GetErrorMessage(638451713, 0);
Trace(2, "事务协调器错误码=", errorMessage16); // 事务协调器错误码=客户端id为空
// 节点网络错误码
string errorMessage17;
errorMessage17 = GetErrorMessage(671089153, 0);
Trace(2, "节点网络错误码=", errorMessage17); // 节点网络错误码=打开路由表文件失败

### 1.9.6 GetErrorMessage()

 string >GetErrorMessage(Int32 errCode,int8 lang) 

 
 
 
 功能描述 
 
 
 
 获取错误码描述 
 
 
 规则约束 
 
 
 enum LanguageCodeEnum
{
LAN_CODE_Local = -1, //获取操作系统语言类型
LAN_CODE_ZH_CN = 0, // default: Simplified Chinese
LAN_CODE_EN_US=1, // English US
LAN_CODE_JA_JP=2, // Japanese
LAN_CODE_KO_KR, // Korean
LAN_CODE_RU_RU, // Russian
LAN_CODE_ZH_HK, // Traditional Chinese(HongKong)
LAN_CODE_ZH_MO, // Traditional Chinese(Macao)
LAN_CODE_ZH_TW, // Traditional Chinese(Taiwan)
}; 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 errCode 
 Int32 
 错误码 
 
 
 lang 
 int8 
 语言类型 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 
 描述，如果按语言类型查不到描述，则返回空 
 
 
 脚本示例 

 // 框架及公共模块错误码
string errorMessage1;
errorMessage1 = GetErrorMessage(536903682, 0);
Trace(2, "框架及公共模块错误码=", errorMessage1); // 框架及公共模块错误码=致命错误
// 模型库错误码
string errorMessage2;
errorMessage2 = GetErrorMessage(-10111, 0);
Trace(2, "模型库错误码=", errorMessage2); // 模型库错误码=数据区ID数据类型不匹配
// 实时库错误码
string errorMessage3;
errorMessage3 = GetErrorMessage(-20052, 0);
Trace(2, "实时库错误码=", errorMessage3); // 实时库错误码=input内函数参数的类型转换失败
// 历史库&计划库错误码
string errorMessage4;
errorMessage4 = GetErrorMessage(-30001, 0);
Trace(2, "历史库&计划库错误码=", errorMessage4); // 历史库&计划库错误码=cloudID超出允许范围
// 图关系库错误码
string errorMessage5;
errorMessage5 = GetErrorMessage(-50000, 0);
Trace(2, "图关系库错误码=", errorMessage5); // 图关系库错误码=批量接口返回部分成功/部分失败
// 空间库错误码
string errorMessage6;
errorMessage6 = GetErrorMessage(906035205, 0);
Trace(2, "空间库错误码=", errorMessage6); // 空间库错误码=获取服务失败
// 接口库错误码
string errorMessage7;
errorMessage7 = GetErrorMessage(-70100, 0);
Trace(2, "接口库错误码=", errorMessage7); // 接口库错误码=接口库_TCP服务端错误
// 资源库错误码
string errorMessage8;
errorMessage8 = GetErrorMessage(-80260, 0);
Trace(2, "资源库错误码=", errorMessage8); // 资源库错误码=获取服务失败
// 视频库错误码
string errorMessage9;
errorMessage9 = GetErrorMessage(-92102, 0);
Trace(2, "视频库错误码getErrorMessage9=", errorMessage9); // 视频库错误码getErrorMessage9=参数类型错误
// 安全库错误码
string errorMessage10;
errorMessage10 = GetErrorMessage(-100003, 0);
Trace(2, "安全库错误码=", errorMessage10); // 安全库错误码=参数类型错误
// 审计库错误码
string errorMessage11;
errorMessage11 = GetErrorMessage(-120002, 0);
Trace(2, "审计库错误码=", errorMessage11); // 审计库错误码=审计库链接异常
// 运维错误码
string errorMessage12;
errorMessage12 = GetErrorMessage(-140003, 0);
Trace(2, "运维错误码=", errorMessage12); // 运维错误码=未找到仓库
// 网关错误码
string errorMessage13;
errorMessage13 = GetErrorMessage(-180999, 0);
Trace(2, "网关错误码=", errorMessage13); // 网关错误码=请求参数无效
// 功能单元库错误码
string errorMessage14;
errorMessage14 = GetErrorMessage(-210069, 0);
Trace(2, "功能单元库错误码=", errorMessage14); // 功能单元库错误码=ID数量与模型版本数量不一致
// 图形库错误码
string errorMessage15;
errorMessage15 = GetErrorMessage(-349999, 0);
Trace(2, "图形库错误码=", errorMessage15); // 图形库错误码=缺失type
// 事务协调器错误码
string errorMessage16;
errorMessage16 = GetErrorMessage(638451713, 0);
Trace(2, "事务协调器错误码=", errorMessage16); // 事务协调器错误码=客户端id为空
// 节点网络错误码
string errorMessage17;
errorMessage17 = GetErrorMessage(671089153, 0);
Trace(2, "节点网络错误码=", errorMessage17); // 节点网络错误码=打开路由表文件失败
 

# 1.9.7 Serialize()

**var >Serialize(blob)**
**blob >Serialize(var)**
var >Serialize(blob)将blob类型转换为元语言类型对象
blob >Serialize(var)将元语言类型对象转换为blob类型
参数名
类型
说明
var,blob
var,blob

返回值
说明
var或blob
转换后的类型
**脚本示例**
// int32类型
int32 a1= 1;
blob b1 = Serialize(a1);
int32 value1 = Serialize(b1);

// string类型
string a2 = "1";
blob b2 = Serialize(a2);
string value2 = Serialize(b2);

// datetime类型
datetime datetimeobj1 = now();
blob b6 = Serialize(datetimeobj1);
datetime datetimeobj2 = Serialize(b6);

// jsonObject类型
jsonObject js1 = {"1":1};
blob b9 = Serialize(js1);
jsonObject js2 = Serialize(b9);

// 数组类型
int32[] intArr1 = {1,100};
blob b1 = Serialize(intArr1);
int32[] intArr2 = Serialize(b1);

// 多维数组
string[][] stringMutilArr1 = {{"1","100"},{"2","3"}};
blob b2 = Serialize(stringMutilArr1);
string[][] stringMutilArr2 = Serialize(b2);

// map类型
int64Map tempMap1;
tempMap1[1]= "value";
blob b51 = Serialize(tempMap1);
int64Map tempMap2 = Serialize(b51);

### 1.9.7 Serialize()

 var >Serialize(blob) 

 blob >Serialize(var) 

 
 
 
 功能描述 
 
 
 
 var >Serialize(blob)将blob类型转换为元语言类型对象 
 
 
 blob >Serialize(var)将元语言类型对象转换为blob类型 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 var,blob 
 var,blob 
 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 var或blob 
 转换后的类型 
 
 
 脚本示例 

 // int32类型
int32 a1= 1;
blob b1 = Serialize(a1);
int32 value1 = Serialize(b1);

// string类型
string a2 = "1";
blob b2 = Serialize(a2);
string value2 = Serialize(b2);

// datetime类型
datetime datetimeobj1 = now();
blob b6 = Serialize(datetimeobj1);
datetime datetimeobj2 = Serialize(b6);

// jsonObject类型
jsonObject js1 = {"1":1};
blob b9 = Serialize(js1);
jsonObject js2 = Serialize(b9);

// 数组类型
int32[] intArr1 = {1,100};
blob b1 = Serialize(intArr1);
int32[] intArr2 = Serialize(b1);

// 多维数组
string[][] stringMutilArr1 = {{"1","100"},{"2","3"}};
blob b2 = Serialize(stringMutilArr1);
string[][] stringMutilArr2 = Serialize(b2);

// map类型
int64Map<string> tempMap1;
tempMap1[1]= "value";
blob b51 = Serialize(tempMap1);
int64Map<string> tempMap2 = Serialize(b51);
 

# 1.9.8 GUID()

**string[] >GUID(uint32 num,bool case)**
生成guid字符串。guid是全局唯一标识符，guid是一个128位二进制数组成（16字节），可用于所有需要唯一标识的计算机和网络。此标识重复的可能性非常小。guid的格式为“xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx”,其中每个x是0-9或a-f范围内的一个十六进制的数字。
参数名
类型
说明
num
uint32
生成guid的个数，不填默认为1，填0按1处理
case
bool
大小写,1：大写；0：小写，不填默认为0
返回值
说明
guid字符串数组
生成的guid字符串数组
**脚本示例**
// 示例1：
string[] guid1;
guid1=GUID(1,0);
Trace(2, "guid1[1]:", guid1[1]); // guid1:d271a596-0b7e-4f89-a593-3edd224f37c2
// 示例2：
string[] guid2;
guid2=GUID(1,1);
Trace(2, "guid2[1]:", guid2[1]); // guid2:11FCD014-71E4-4106-B08A-E54E395E2E07
// 示例3：
string[] guid3;
guid3=GUID(); // 两参数为空暂未支持，必须都填上
Trace(2, "guid3[1]:", guid3[1]); // guid2:9dd7029b-fae1-484c-9c4a-e0680a9a6db9

### 1.9.8 GUID()

 string[] >GUID(uint32 num,bool case) 

 
 
 
 功能描述 
 
 
 
 生成guid字符串。guid是全局唯一标识符，guid是一个128位二进制数组成（16字节），可用于所有需要唯一标识的计算机和网络。此标识重复的可能性非常小。guid的格式为“xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx”,其中每个x是0-9或a-f范围内的一个十六进制的数字。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 num 
 uint32 
 生成guid的个数，不填默认为1，填0按1处理 
 
 
 case 
 bool 
 大小写,1：大写；0：小写，不填默认为0 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 guid字符串数组 
 生成的guid字符串数组 
 
 
 脚本示例 

 // 示例1：
string[] guid1;
guid1=GUID(1,0);
Trace(2, "guid1[1]:", guid1[1]); // guid1:d271a596-0b7e-4f89-a593-3edd224f37c2
// 示例2：
string[] guid2;
guid2=GUID(1,1);
Trace(2, "guid2[1]:", guid2[1]); // guid2:11FCD014-71E4-4106-B08A-E54E395E2E07
// 示例3：
string[] guid3;
guid3=GUID(); // 两参数为空暂未支持，必须都填上
Trace(2, "guid3[1]:", guid3[1]); // guid2:9dd7029b-fae1-484c-9c4a-e0680a9a6db9
 

# 1.9.9 Hash()

**string >Hash(string text，int32 algorithm，int32 code)**
获取字符串的哈希值。
参数名
类型
说明
text
string
字符串文本内容
algorithm
int32
算法，0：sha-256; 1：sm3；2：md5
code
int32
编码，0：utf8; 1:base64; 2:hex
返回值
说明
string
字符串的哈希值
**脚本示例**
string text="你好";
string sha256Value=Hash(text,0,1);
Trace(2, "sha256Value=", sha256Value); 

### 1.9.9 Hash()

 string >Hash(string text，int32 algorithm，int32 code) 

 
 
 
 功能描述 
 
 
 
 获取字符串的哈希值。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 text 
 string 
 字符串文本内容 
 
 
 algorithm 
 int32 
 算法，0：sha-256; 1：sm3；2：md5 
 
 
 code 
 int32 
 编码，0：utf8; 1:base64; 2:hex 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 字符串的哈希值 
 
 
 脚本示例 

 string text="你好";
string sha256Value=Hash(text,0,1);
Trace(2, "sha256Value=", sha256Value); 
 

# 1.9.10 Hmac()

**string >Hmac(string key, bool keyType，string text，int32 algorithm，int32 code)**
根据密钥和字符串文本内容，通过哈希算法生成mac码。
参数名
类型
说明
key
string
密钥
keyType
bool
0:公钥：1：私钥，默认为0
text
string
字符串文本内容
algorithm
int32
算法，0：sha-256; 暂支持sha-256算法
code
int32
编码，0：utf8; 1:base64; 2:hex
返回值
说明
string
mac码
**脚本示例**
// 生成密钥对
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];
// 根据密钥和字符串文本内容，通过hash函数生成mac码
string text="你好";
string macValue=Hmac(pubkey,0,text,0,1);

### 1.9.10 Hmac()

 string >Hmac(string key, bool keyType，string text，int32 algorithm，int32 code) 

 
 
 
 功能描述 
 
 
 
 根据密钥和字符串文本内容，通过哈希算法生成mac码。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 key 
 string 
 密钥 
 
 
 keyType 
 bool 
 0:公钥：1：私钥，默认为0 
 
 
 text 
 string 
 字符串文本内容 
 
 
 algorithm 
 int32 
 算法，0：sha-256; 暂支持sha-256算法 
 
 
 code 
 int32 
 编码，0：utf8; 1:base64; 2:hex 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 string 
 mac码 
 
 
 脚本示例 

 // 生成密钥对
int32 bits=512;
string[] key;
key=GenRsaKey(bits);
string pubkey=key[1];
string prikey=key[2];
// 根据密钥和字符串文本内容，通过hash函数生成mac码
string text="你好";
string macValue=Hmac(pubkey,0,text,0,1);
 

# 1.9.11 Ping()

**int32 >Ping(string ip)**
执行系统Ping命令。默认只Ping一次，默认超时时间10s。
参数名
类型
说明
ip
string
IP地址，不符合规范报错
返回值
说明
int32
命令执行结果，成功为0，失败为-1错误码
**脚本示例**
// 网络诊断：ping
string ip = "127.1.1.1";
int32 res = Ping(ip); //res=0,表示ping通

### 1.9.11 Ping()

 int32 >Ping(string ip) 

 
 
 
 功能描述 
 
 
 
 执行系统Ping命令。默认只Ping一次，默认超时时间10s。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 ip 
 string 
 IP地址，不符合规范报错 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 int32 
 命令执行结果，成功为0，失败为-1错误码 
 
 
 脚本示例 

 // 网络诊断：ping
string ip = "127.1.1.1";
int32 res = Ping(ip); //res=0,表示ping通
 

# 1.9.12 LexicalAnalysis()

**int32 >LexicalAnalysis(int32 type,string charStream,token[]&tokens);**
词法分析，即将字符序列转换为单词（Token）序列的过程。
接收字符串形式的源程序，按照源程序输入的次序依次扫描源程序，在扫描的同时根据语言的词法规则识别出具有独立意义的单词，并产生与源程序等价的属性字（Token）流。
参数名
类型
说明
type
int32
类型，1：元语言，2：Js
charStream
string
字符流
**输出参数说明**

tokens
token[]
单词
tokens
{
int32 typeCode; //类型码
String tokenvar; //单词符号
int32 row; //行号，从1开始
int32 col; //列号，从1开始
int32 order; //顺序，从1开始
}
*注意token为内置结构体，不需要用户自己申明
返回值
说明
0
成功
-1
脚本内容无效或为空
-2
词法解析错误
-3
词法解析异常
-4
语法解析错误
-5
语法解析异常
脚本示例：
int32 type=1;
string charStream="int32 sum;\nint32 a=1;\nint32 b=2;\nsum=a+b;";
token[] tokens;
int32 code=LexicalAnalysis(type,charStream,tokens);
/*
 tokens[19]={
 {1,"int32",1,1,1},
 {901,"sum",1,7,2},
 {904,";".1,10,3},
 {1,"int32",2,1,4},
 {901,"a",2,7,5},
 {903,"=",2,8,6},
 {902,"1",2,9.7},
 {904,";",2,10,8},
 {1,"int32",3,1,9},
 {901,"b",3,7,10},
 {903,"=",3,8,11},
 {902,"2",3,9,12},
 {904,";",3,10,13},
 {901,"sum",4,1,14},
 {903,"=",4,4,15},
 {901,"a",4.5,16},
 {903,"+",4.6,17},
 {901,"b",4.7,18},
 {904,";",4,8,19},
 };
*/
词法分析中单词符号主要分为：关键字、标识符、常数、运算符、分界符5类。
单词符号分类码表
**元语言：**return、continue、break、if、else、switch、case、default、for、while、do、thisFU、char、bool、true、false、int、int8、uint8、int16、uint16、int32、uint32、int64、uint64、float、double、decimal、string、blob、array、datetime、timespan、struct、jsonobject、var、enum、int64map、int64MapIterator、uint64map、uint64MapIterator、stringmap、stringMapIterator、dataSet
**图形、空间库：**PhysicScreen、Caret、Mouse、Clipboard、Focus、Point2D、Line2D、Polyline2D、BezierCurve2D、SplineCurve2D、Hyperbola2D、Parabola2D、CircularInvolute2D、Clothoid2D、CircularString2D、TrimmedCurve2D、OffsetCurve2D、Rect2D、RoundRect2D、Polygon2D、Ellipse2D、Chord2D、Pie2D、CurvePolygon2D、Text2D、Image2D、Group2D、Point3D、Line3D、Polyline3D、EllipseArc3D、BezierCurve3D、SplineCurve3D、Hyperbola3D、Parabola3D、CircularInvolute3D、Clothoid3D、Rect3D、RoundRect3D、Polygon3D、SphericalSurface、CylindricalSurface、ConicalSurface、ToroidalSurface、SplineSurface、BlockVolum、SphericalVolume、WedgeVolume、ToroidalVolume、CircularTruncatedCone、CylindricalVolume、ConicalVolume、PrismVolume、Frustum、PyramidVolume、PyramidVolume4、TetrahedronVolume、Text3D、Circularstring3D、CurvePolygon3D、Image3D、SolidBrush、TextureBrush、LinearGradientBrush、RadialGradientBrush、ConicalGradientBrush、HatchBrush、Pen、Font、Color、OrthographicCamera、PerspectiveCamera、AmbientLight、DirectionalLight、PointLight、SpotLight、MeshBasicMaterial、MeshLambertMaterial、MeshPhongMaterial、MeshStandardMaterial、PointsMaterial、LineBasicMaterial、LineDashMaterial、Textures、EllipseArc2D、Pipe2D、Ellipse3D、Pie3D、EllipsoidVolume、HexahedronVolume、Group3D、TriMesh、Model3D、PointM、MultiPoint2D、MultiPointM、LinearRing2D、LinearRingM、LineString2D、LineStringM、MultiLineString2D、MultiLineStringM、Polygon2D_GIS、PolygonM、MultiPolygon2D、MultiPolygonM、GeometryCollection2D、PointZM、MultiPoint3D、MultiPointZM、LinearRing3D、LinearRingZM、LineString3D、LineStringZM、MultiLineString3D、MultiLineStringZM、Polygon3D_GIS、PolygonZM、MultiPolygon3D、MultiPolygonZM、MultiPatch、MultiPatchZM、TIN、TINZM、GeometryCollection3D、Direction2D、Vector2D、Axis2Placement2D、Circle2D、Circle2D_Points、Circle2D_TangentsR、Circle2D_Tangents、Ellipse2D_CAD、Ellipse2D_centerPoint、Ellipse2D_points、Hyperbola2D_CAD、Parabola2D_CAD、Clothoid2D_CAD、CircularInvolute2D_CAD、CartesianTransformationOperator2D、Direction3D、Vector3D、Axis2Placement3D、Ellipse3D_CAD、Ellipse3D_centerPoint、Ellipse3D_points、Hyperbola3D_CAD、Parabola3D_CAD、Clothoid3D_CAD、CircularInvolute3D_CAD、TrimmedCurve3D、CylindricalSurface_CAD、ConicalSurface_CAD、SphericalSurface_CAD、SplineSurface_CAD、ToroidalSurface_CAD、BlockVolume_CAD、WedgeVolume_CAD、PyramidVolume_CAD、SphericalVolume_CAD、CylindricalVolume_CAD、EccentricConicalVolume、ConicalVolume_CAD、ToroidalVolume_CAD、EllipsoidVolume_CAD、Pyramid_CAD、Prism_CAD、Cylinder_CAD、Frustum_CAD、RegularPolyhedron、SolidGeneratedBySurfaceExtrusion、SolidGeneratedByCurveSweep、FilletingSolidOfBasicShapes、FilletingSolidOfBasicShapes_3Dfaces、SolidGeneratedByBendingBasicForm、SolidGeneratedByClippingBasicForm、ChamferingSolidOfBasicShapes_2Dedges、ChamferingSolidOfBasicShapes_3Dedges、ChamferingSolidOfBasicShapes_3Dfaces、SolidGeneratedByCompoundOperation、CartesianTransformationOperator3D
1
关键字
**WebJs：**abstract、any、as、asserts、bigint、boolean、catch、class、const、constructor、debugger、declare、delete、export、extends、finally、from、function、get、implements、import、in、infer、instanceof、interface、is、keyof、let、module、namespace、never、new、null、number、object、out、package、protected、override、readonly、require、global、satisfies、set、static、super、symbol、throw、try、type、typeof、undefined、unique、unknown、void、with、yield、async、await、of
1
关键字
标识符（字母数字串）
901
标识符
常数（0-9数字串，常量字符串）
902
常数
=、+=、-=、\*=、/=、%=、 >=、&=、|=、\^=、+、-、\*、%、++、--、/、|、^、\~、 >、 、 =、==、 >=、&&、||、.
903
运算符
;,{}:()[]::
904
分界符

### 1.9.12 LexicalAnalysis()

 int32 >LexicalAnalysis(int32 type,string charStream,token[]&tokens); 

 
 
 
 功能描述 
 
 
 
 词法分析，即将字符序列转换为单词（Token）序列的过程。
接收字符串形式的源程序，按照源程序输入的次序依次扫描源程序，在扫描的同时根据语言的词法规则识别出具有独立意义的单词，并产生与源程序等价的属性字（Token）流。 
 
 
 
 
 
 输入参数说明 
 
 
 
 
 
 参数名 
 类型 
 说明 
 
 
 type 
 int32 
 类型，1：元语言，2：Js 
 
 
 charStream 
 string 
 字符流 
 
 
 输出参数说明 
 
 
 
 
 tokens 
 token[] 
 单词
tokens
{
int32 typeCode; //类型码
String tokenvar; //单词符号
int32 row; //行号，从1开始
int32 col; //列号，从1开始
int32 order; //顺序，从1开始
}
*注意token为内置结构体，不需要用户自己申明 
 
 
 
 
 
 返回值 
 
 
 
 
 返回值 
 说明 
 
 
 0 
 成功 
 
 
 -1 
 脚本内容无效或为空 
 
 
 -2 
 词法解析错误 
 
 
 -3 
 词法解析异常 
 
 
 -4 
 语法解析错误 
 
 
 -5 
 语法解析异常 
 
 
 脚本示例：

 int32 type=1;
string charStream="int32 sum;\nint32 a=1;\nint32 b=2;\nsum=a+b;";
token[] tokens;
int32 code=LexicalAnalysis(type,charStream,tokens);
/*
 tokens[19]={
 {1,"int32",1,1,1},
 {901,"sum",1,7,2},
 {904,";".1,10,3},
 {1,"int32",2,1,4},
 {901,"a",2,7,5},
 {903,"=",2,8,6},
 {902,"1",2,9.7},
 {904,";",2,10,8},
 {1,"int32",3,1,9},
 {901,"b",3,7,10},
 {903,"=",3,8,11},
 {902,"2",3,9,12},
 {904,";",3,10,13},
 {901,"sum",4,1,14},
 {903,"=",4,4,15},
 {901,"a",4.5,16},
 {903,"+",4.6,17},
 {901,"b",4.7,18},
 {904,";",4,8,19},
 };
*/
 
 词法分析中单词符号主要分为：关键字、标识符、常数、运算符、分界符5类。

 单词符号分类码表

 
 
 
 单词符号 
 编码 
 类别 
 
 
 
 元语言： return、continue、break、if、else、switch、case、default、for、while、do、thisFU、char、bool、true、false、int、int8、uint8、int16、uint16、int32、uint32、int64、uint64、float、double、decimal、string、blob、array、datetime、timespan、struct、jsonobject、var、enum、int64map、int64MapIterator、uint64map、uint64MapIterator、stringmap、stringMapIterator、dataSet
 图形、空间库： PhysicScreen、Caret、Mouse、Clipboard、Focus、Point2D、Line2D、Polyline2D、BezierCurve2D、SplineCurve2D、Hyperbola2D、Parabola2D、CircularInvolute2D、Clothoid2D、CircularString2D、TrimmedCurve2D、OffsetCurve2D、Rect2D、RoundRect2D、Polygon2D、Ellipse2D、Chord2D、Pie2D、CurvePolygon2D、Text2D、Image2D、Group2D、Point3D、Line3D、Polyline3D、EllipseArc3D、BezierCurve3D、SplineCurve3D、Hyperbola3D、Parabola3D、CircularInvolute3D、Clothoid3D、Rect3D、RoundRect3D、Polygon3D、SphericalSurface、CylindricalSurface、ConicalSurface、ToroidalSurface、SplineSurface、BlockVolum、SphericalVolume、WedgeVolume、ToroidalVolume、CircularTruncatedCone、CylindricalVolume、ConicalVolume、PrismVolume、Frustum、PyramidVolume、PyramidVolume4、TetrahedronVolume、Text3D、Circularstring3D、CurvePolygon3D、Image3D、SolidBrush、TextureBrush、LinearGradientBrush、RadialGradientBrush、ConicalGradientBrush、HatchBrush、Pen、Font、Color、OrthographicCamera、PerspectiveCamera、AmbientLight、DirectionalLight、PointLight、SpotLight、MeshBasicMaterial、MeshLambertMaterial、MeshPhongMaterial、MeshStandardMaterial、PointsMaterial、LineBasicMaterial、LineDashMaterial、Textures、EllipseArc2D、Pipe2D、Ellipse3D、Pie3D、EllipsoidVolume、HexahedronVolume、Group3D、TriMesh、Model3D、PointM、MultiPoint2D、MultiPointM、LinearRing2D、LinearRingM、LineString2D、LineStringM、MultiLineString2D、MultiLineStringM、Polygon2D_GIS、PolygonM、MultiPolygon2D、MultiPolygonM、GeometryCollection2D、PointZM、MultiPoint3D、MultiPointZM、LinearRing3D、LinearRingZM、LineString3D、LineStringZM、MultiLineString3D、MultiLineStringZM、Polygon3D_GIS、PolygonZM、MultiPolygon3D、MultiPolygonZM、MultiPatch、MultiPatchZM、TIN、TINZM、GeometryCollection3D、Direction2D、Vector2D、Axis2Placement2D、Circle2D、Circle2D_Points、Circle2D_TangentsR、Circle2D_Tangents、Ellipse2D_CAD、Ellipse2D_centerPoint、Ellipse2D_points、Hyperbola2D_CAD、Parabola2D_CAD、Clothoid2D_CAD、CircularInvolute2D_CAD、CartesianTransformationOperator2D、Direction3D、Vector3D、Axis2Placement3D、Ellipse3D_CAD、Ellipse3D_centerPoint、Ellipse3D_points、Hyperbola3D_CAD、Parabola3D_CAD、Clothoid3D_CAD、CircularInvolute3D_CAD、TrimmedCurve3D、CylindricalSurface_CAD、ConicalSurface_CAD、SphericalSurface_CAD、SplineSurface_CAD、ToroidalSurface_CAD、BlockVolume_CAD、WedgeVolume_CAD、PyramidVolume_CAD、SphericalVolume_CAD、CylindricalVolume_CAD、EccentricConicalVolume、ConicalVolume_CAD、ToroidalVolume_CAD、EllipsoidVolume_CAD、Pyramid_CAD、Prism_CAD、Cylinder_CAD、Frustum_CAD、RegularPolyhedron、SolidGeneratedBySurfaceExtrusion、SolidGeneratedByCurveSweep、FilletingSolidOfBasicShapes、FilletingSolidOfBasicShapes_3Dfaces、SolidGeneratedByBendingBasicForm、SolidGeneratedByClippingBasicForm、ChamferingSolidOfBasicShapes_2Dedges、ChamferingSolidOfBasicShapes_3Dedges、ChamferingSolidOfBasicShapes_3Dfaces、SolidGeneratedByCompoundOperation、CartesianTransformationOperator3D 
 1 
 关键字 
 
 
 WebJs： abstract、any、as、asserts、bigint、boolean、catch、class、const、constructor、debugger、declare、delete、export、extends、finally、from、function、get、implements、import、in、infer、instanceof、interface、is、keyof、let、module、namespace、never、new、null、number、object、out、package、protected、override、readonly、require、global、satisfies、set、static、super、symbol、throw、try、type、typeof、undefined、unique、unknown、void、with、yield、async、await、of 
 1 
 关键字 
 
 
 标识符（字母数字串） 
 901 
 标识符 
 
 
 常数（0-9数字串，常量字符串） 
 902 
 常数 
 
 
 =、+=、-=、*=、/=、%=、<<=、>>=、&=、|=、^=、+、-、*、%、++、--、/、|、^、~、<<、>>、<、>、<=、>=、==、<<=、>>=、&&、||、. 
 903 
 运算符 
 
 
 ;,{}:()[]:: 
 904 
 分界符