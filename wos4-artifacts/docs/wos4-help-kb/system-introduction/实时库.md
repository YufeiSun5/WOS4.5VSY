# 实时库

- 分组：系统介绍
- 原始章节名：实时库
- 内容块数量：41
- 来源：WOS4 帮助手册 static/js/file.js

## 正文

# 基础概念

# 基础概念

# 实时数据

WellinOS提供将客观世界万事万物快速转化为数字孪生体的“数字孪生”方法，并提供管理数字孪生体全生命周期的功能。
WellinOS通过管理数据对象的三种时态对象：实时对象、历史对象、计划对象，实现对数字孪生体的全生命周期的管理功能。
实时库是存储和管理实时对象的数据库，部分实时对象还包括数据记录。实时库提供实时对象、记录的存储、修改、查询、订阅等功能。用户可以通过APP访问实时库中的实时对象及其记录。

## 实时数据

 WellinOS提供将客观世界万事万物快速转化为数字孪生体的“数字孪生”方法，并提供管理数字孪生体全生命周期的功能。

 WellinOS通过管理数据对象的三种时态对象：实时对象、历史对象、计划对象，实现对数字孪生体的全生命周期的管理功能。

 实时库是存储和管理实时对象的数据库，部分实时对象还包括数据记录。实时库提供实时对象、记录的存储、修改、查询、订阅等功能。用户可以通过APP访问实时库中的实时对象及其记录。

# 模型/拷贝/对象/记录

在时空库进行数字孪生的过程：
Step1，构建数据模型。
Step2，根据数据模型生成数据拷贝。
Step3，根据数据拷贝生成数据对象（实时/历史/计划），即客观对象的数字孪生体对象。
Step4，为对象添加数据记录。（并非所有对象都支持记录）
模型是数据的“基因”，模型即业务关心的客观事物对象的参数集合，模型决定数据的结构；
拷贝是“基因”的载体，基因（模型）必须有载体，才能“传播”和使用。拷贝为模型的参数结构加一组参数值，拷贝是运行对象的配置数据。拷贝是根据模型创建的，一个模型版本可以生成多个拷贝。
模型和拷贝是在模型库创建的，通过时空部署功能部署到实时库，或者通过导入功能导入到实时库。实时库提供简单的模型、拷贝管理功能，详情参见相关的功能点。
对象即客观事物对应的数字孪生体；对象是根据拷贝创建的。（广义上，任何目标数据都可称为对象，比如模型也是一种对象；此处指时空库概念中狭义上的对象）
对象分为实时对象、历史对象、计划对象，分别表示客观事物的实时、历史、计划状态。
历史对象包括历史记录，计划对象包括计划记录，实时时序事、业务事对象也有记录。数据记录属于数据对象，依附于数据对象而存在，要想添加数据记录，首先要创建数据对象，删除对象，会一并将对象的记录删除。但并不是所有类型的对象都有记录，比如实时物对象、资源对象等就没有记录。实时库中，只有实时业务事对象和实时时序事对象才有记录。

## 模型/拷贝/对象/记录

 在时空库进行数字孪生的过程：

 Step1，构建数据模型。

 Step2，根据数据模型生成数据拷贝。

 Step3，根据数据拷贝生成数据对象（实时/历史/计划），即客观对象的数字孪生体对象。

 Step4，为对象添加数据记录。（并非所有对象都支持记录）

 模型是数据的“基因”，模型即业务关心的客观事物对象的参数集合，模型决定数据的结构；

 拷贝是“基因”的载体，基因（模型）必须有载体，才能“传播”和使用。拷贝为模型的参数结构加一组参数值，拷贝是运行对象的配置数据。拷贝是根据模型创建的，一个模型版本可以生成多个拷贝。

 模型和拷贝是在模型库创建的，通过时空部署功能部署到实时库，或者通过导入功能导入到实时库。实时库提供简单的模型、拷贝管理功能，详情参见相关的功能点。

 对象即客观事物对应的数字孪生体；对象是根据拷贝创建的。（广义上，任何目标数据都可称为对象，比如模型也是一种对象；此处指时空库概念中狭义上的对象）

 对象分为实时对象、历史对象、计划对象，分别表示客观事物的实时、历史、计划状态。

 历史对象包括历史记录，计划对象包括计划记录，实时时序事、业务事对象也有记录。数据记录属于数据对象，依附于数据对象而存在，要想添加数据记录，首先要创建数据对象，删除对象，会一并将对象的记录删除。但并不是所有类型的对象都有记录，比如实时物对象、资源对象等就没有记录。实时库中，只有实时业务事对象和实时时序事对象才有记录。

# 物

在时空数据库中，物是客观世界中物理实体的数字孪生体，比如汽车、轮胎等都是物。
物的分类：
ü 时序物：又分为单灵敏度时序物、多灵敏度时序物。
时序物主要指变化较频繁的物，其数据需要频繁地写库，单值频繁写库使用单灵敏度时序物，比如传感器；多值一同频繁写库使用多灵敏度时序物，比如采集设备（如定时采集温度、压力、流量等多个值）。时序物有自己独特的属性：新鲜期（实时）、有效期（历史），单灵敏度时序物还有变化死区（实时）、存储死区（历史）等，请参阅相关术语概念。
单灵敏度时序物分为实时单灵敏度时序物和历史单灵敏度时序物，没有计划单灵敏度时序物。
多灵敏度时序物分为实时多灵敏度时序物和历史多灵敏度时序物，没有计划多灵敏度时序物。
时序历史物支持记录，时序实时物不支持记录；
时序物的父是业务物。
ü 业务物：
业务物不具有时序物的一些特性；
业务物分为业务实时物、业务历史物、业务计划物；
业务历史物和业务计划物支持记录，业务实时物不支持记录；

## 物

 在时空数据库中，物是客观世界中物理实体的数字孪生体，比如汽车、轮胎等都是物。

 物的分类：

 ü 时序物：又分为单灵敏度时序物、多灵敏度时序物。

 时序物主要指变化较频繁的物，其数据需要频繁地写库，单值频繁写库使用单灵敏度时序物，比如传感器；多值一同频繁写库使用多灵敏度时序物，比如采集设备（如定时采集温度、压力、流量等多个值）。时序物有自己独特的属性：新鲜期（实时）、有效期（历史），单灵敏度时序物还有变化死区（实时）、存储死区（历史）等，请参阅相关术语概念。

 单灵敏度时序物分为实时单灵敏度时序物和历史单灵敏度时序物，没有计划单灵敏度时序物。

 多灵敏度时序物分为实时多灵敏度时序物和历史多灵敏度时序物，没有计划多灵敏度时序物。

 时序历史物支持记录，时序实时物不支持记录；

 时序物的父是业务物。

 ü 业务物：

 业务物不具有时序物的一些特性；

 业务物分为业务实时物、业务历史物、业务计划物；

 业务历史物和业务计划物支持记录，业务实时物不支持记录；

# 事

在时空数据库中，事是客观世界中的事件的数字孪生体，比如订单、工单、报警等都是事。
事的分类：
ü 时序事：
时序事主要指高频存储、变化的事对象，比如设备报警。
时序事分为实时时序事和历史时序事，没有计划时序事；
时序事对象支持记录；
时序事记录没有父记录；
ü 业务事：
业务事分为实时业务事、历史业务事和计划业务事。
业务事对象支持记录；
业务事记录有父记录；

## 事

 在时空数据库中，事是客观世界中的事件的数字孪生体，比如订单、工单、报警等都是事。

 事的分类：

 ü 时序事：

 时序事主要指高频存储、变化的事对象，比如设备报警。

 时序事分为实时时序事和历史时序事，没有计划时序事；

 时序事对象支持记录；

 时序事记录没有父记录；

 ü 业务事：

 业务事分为实时业务事、历史业务事和计划业务事。

 业务事对象支持记录；

 业务事记录有父记录；

# 组织

在时空数据库中，组织是客观世界中的组织机构的数字孪生体，例如XX公司、XX公司下的部门等都是组织。
组织分为实时组织、历史组织、计划组织。
历史、计划组织对象有记录，实时组织对象没有记录。

## 组织

 在时空数据库中，组织是客观世界中的组织机构的数字孪生体，例如XX公司、XX公司下的部门等都是组织。

 组织分为实时组织、历史组织、计划组织。

 历史、计划组织对象有记录，实时组织对象没有记录。

# 岗位

在时空数据库中，岗位是客观世界中的组织下的岗位角色的数字孪生体，例如XX公司的董事长、总经理等都是岗位。
岗位分为实时岗位、历史岗位、计划岗位。
历史、计划岗位对象有记录，实时岗位对象没有记录。
岗位的父是组织。

## 岗位

 在时空数据库中，岗位是客观世界中的组织下的岗位角色的数字孪生体，例如XX公司的董事长、总经理等都是岗位。

 岗位分为实时岗位、历史岗位、计划岗位。

 历史、计划岗位对象有记录，实时岗位对象没有记录。

 岗位的父是组织。

# 人员

在时空数据库中，客观世界中的人的数字孪生体是人员，例如XX公司中的人、XX公司下的部门中的人等都是人员。
人员具有自己独特的属性：组织角色。
人员分为实时人员、历史人员、计划人员。
历史、计划人员对象有记录，实时人员对象没有记录。

## 人员

 在时空数据库中，客观世界中的人的数字孪生体是人员，例如XX公司中的人、XX公司下的部门中的人等都是人员。

 人员具有自己独特的属性：组织角色。

 人员分为实时人员、历史人员、计划人员。

 历史、计划人员对象有记录，实时人员对象没有记录。

# 资源

## 资源

# 时空

时空是一个容器，其中包含业务应用相关的内容（程序+数据）。实时库的对象，都处于时空当中。
对象的操作有时空约束，对象的安全鉴权也受时空范围锁的约束，时空之间彼此隔离，容纳不同业务模块的程序和数据。
时空是用一种对象描述的，即时空对象；
在实时库中，可以创建、删除、修改、查询时空对象；
实时库中的时空对象还可以是从模型库部署过来的。
\ APP访问数据限制说明（本云访问）：
（1）APP写数据（增、删、改）时，受APP部署时空的限制，只能操作功能单元部署时空内的数据；
（2）APP读数据（查询、订阅）时，不受APP部署时空的限制，可以访问任意时空内的数据；
\ APP访问数据限制说明（跨云访问）：
（1）APP读、写数据（增、删、改、查询、订阅）时，不受时空的限制，可以访问指定云区域的任意时空（通过在APP上指定）；

## 时空

 时空是一个容器，其中包含业务应用相关的内容（程序+数据）。实时库的对象，都处于时空当中。

 对象的操作有时空约束，对象的安全鉴权也受时空范围锁的约束，时空之间彼此隔离，容纳不同业务模块的程序和数据。

 时空是用一种对象描述的，即时空对象；

 在实时库中，可以创建、删除、修改、查询时空对象；

 实时库中的时空对象还可以是从模型库部署过来的。

 \ APP访问数据限制说明（本云访问）：

 （1）APP写数据（增、删、改）时，受APP部署时空的限制，只能操作功能单元部署时空内的数据；

 （2）APP读数据（查询、订阅）时，不受APP部署时空的限制，可以访问任意时空内的数据；

 \ APP访问数据限制说明（跨云访问）：

 （1）APP读、写数据（增、删、改、查询、订阅）时，不受时空的限制，可以访问指定云区域的任意时空（通过在APP上指定）；

# 时间坐标系

时间坐标系是用一个对象描述的，即时间对象，时间对象是在模型库中创建的；
时间坐标系描述一个绝对时间轴从起始时间开始排布成连续时间期间的规则，
时间坐标系上面有配置：开始时间，
例子：
开始时间：2020-8（datetime）
Ø 时间表示： 
 时空库用一个整数表示一个时间；
​ 假设整数为x，
 含义：相对于时间坐标系的开始时间（datetime），偏移x个100ns；
 物的时间戳，事的开始时间、结束时间，都是这样表示的；
Ø 系统时间坐标系： 
 wos中具有系统时间坐标系的概念，系统时间坐标系的开始时间为1601-1-1 0:0:0utc
Ø 注意事项
TRGUID可以为空串，表示使用系统时间坐标系；
注，时空下的数据如果不使用系统时间坐标系（0），就只能使用时空下的时间坐标系，不能使用父时空、子时空、孙时空....等其他时空下的时间坐标系；
运行库均采用系统时间坐标系，即使trguid不为空串；

## 时间坐标系

 时间坐标系是用一个对象描述的，即时间对象，时间对象是在模型库中创建的；

 时间坐标系描述一个绝对时间轴从起始时间开始排布成连续时间期间的规则，

 时间坐标系上面有配置：开始时间，

 例子：

 开始时间：2020-8（datetime）

 Ø 时间表示： 

 时空库用一个整数表示一个时间；

 ​ 假设整数为x，

 含义：相对于时间坐标系的开始时间（datetime），偏移x个100ns；

 物的时间戳，事的开始时间、结束时间，都是这样表示的；

 Ø 系统时间坐标系： 

 wos中具有系统时间坐标系的概念，系统时间坐标系的开始时间为1601-1-1 0:0:0utc

 Ø 注意事项

 TRGUID可以为空串，表示使用系统时间坐标系；

 注，时空下的数据如果不使用系统时间坐标系（0），就只能使用时空下的时间坐标系，不能使用父时空、子时空、孙时空....等其他时空下的时间坐标系；

 运行库均采用系统时间坐标系，即使trguid不为空串；

# 云区域/集群/数据区/时空/对象

在wos中，具有云区域、集群、数据区、时空、对象的概念，它们的层级关系，示意如下，

-集群

-数据区

-时空

-仓库

-模型

-拷贝

-资源对象

-数据对象

-数据对象的别名

-资源对象

-时空的别名

其中，云和数据区属于访问的环境参数，所有的操作都需要进行指定。
区域、集群、数据区的概念请参阅平台系统的内容。

## 云区域/集群/数据区/时空/对象

 在wos中，具有云区域、集群、数据区、时空、对象的概念，它们的层级关系，示意如下，

 
 
 
 -云区域 
 
 
 
 
 
 
 
 
 
 -集群 
 
 
 
 
 
 
 
 
 -数据区 
 
 
 
 
 
 
 
 
 -时空 
 
 
 
 
 
 
 
 
 -仓库 
 
 
 
 
 
 
 
 
 -模型 
 
 
 
 
 
 
 
 -拷贝 
 
 
 
 
 
 
 
 -资源对象 
 
 
 
 
 
 
 -数据对象 
 
 
 
 
 
 
 
 -数据对象的别名 
 
 
 
 
 
 
 
 -资源对象 
 
 
 
 
 
 
 -时空的别名 
 
 
 
 
 其中，云和数据区属于访问的环境参数，所有的操作都需要进行指定。

 区域、集群、数据区的概念请参阅平台系统的内容。

# 父子/祖先/后代

数据对象间组成树状，即数据对象之间有分支，并且具有层次关系的结构，可用于表示数据对象之间存在的一对多关系。如下图，每个“树”的节点比如A、B、C...都代表一数量据对象，数据对象间的关系形成“树”形。在树形结构中，树根节点，如图中的A节点，没有前驱节点（父节点），其余每个节点有且只有一个前驱节点（父节点）。叶子节点，如图中的E、K、G、H 、L、M、J节点，没有后续节点（子节点），其余每个节点的后续节点（子节点）可以是一个，也可以是多个，如图：
![img](系统说明.assets/wpsABFtmp.jpg) 
Ø 树节点（Tree Node）：树中的一个独立单元，上图中的每一个圆形所表示的都是一个节点。
Ø 树根（Root）：位于树顶部的节点，也称顶级节点，如上图中的A节点。
Ø 节点的祖先和后代：一个节点的祖先包括父节点、祖父节点、曾祖父节点等，后代包括子节点、孙节点、曾孙节点等，如上图中，节点I的祖先节点有节点D和节点A，后代节点有节点L和节点M。每一个节点和它的后代也构成一棵树。
Ø 层级（Level）：如上图，一个节点和它的后代树，节点是第0层级（level为0）；节点的子第一层级（level为1），节点的孙是第二级（level为2）...
距离是层级的另外一种说法，二者值一样，但是角度不同，如下，
![img](实时库介绍.assets/wpsE0CC.tmp.png)
距离，指后代距离祖先的分隔段数，中间隔着1个对象，分隔段数为2，中间隔着两个对象，分隔段数为3...........；
假设父-子关系如上图，则有：
node1与node1的距离是0；
node2、node3与node1的距离是1；
node4~node6与node1的距离是2；
距离也叫深度、层级；
注意，距离指的是后代到祖先的距离，而不是祖先到后代的距离, 所以distance值一定是非负数；

## 父子/祖先/后代

 数据对象间组成树状，即数据对象之间有分支，并且具有层次关系的结构，可用于表示数据对象之间存在的一对多关系。如下图，每个“树”的节点比如A、B、C...都代表一数量据对象，数据对象间的关系形成“树”形。在树形结构中，树根节点，如图中的A节点，没有前驱节点（父节点），其余每个节点有且只有一个前驱节点（父节点）。叶子节点，如图中的E、K、G、H 、L、M、J节点，没有后续节点（子节点），其余每个节点的后续节点（子节点）可以是一个，也可以是多个，如图：

 

 Ø 树节点（Tree Node）：树中的一个独立单元，上图中的每一个圆形所表示的都是一个节点。

 Ø 树根（Root）：位于树顶部的节点，也称顶级节点，如上图中的A节点。

 Ø 节点的祖先和后代：一个节点的祖先包括父节点、祖父节点、曾祖父节点等，后代包括子节点、孙节点、曾孙节点等，如上图中，节点I的祖先节点有节点D和节点A，后代节点有节点L和节点M。每一个节点和它的后代也构成一棵树。

 Ø 层级（Level）：如上图，一个节点和它的后代树，节点是第0层级（level为0）；节点的子第一层级（level为1），节点的孙是第二级（level为2）...

 距离是层级的另外一种说法，二者值一样，但是角度不同，如下，

 

 距离，指后代距离祖先的分隔段数，中间隔着1个对象，分隔段数为2，中间隔着两个对象，分隔段数为3...........；

 假设父-子关系如上图，则有：

 node1与node1的距离是0；

 node2、node3与node1的距离是1；

 node4~node6与node1的距离是2；

 距离也叫深度、层级；

 注意，距离指的是后代到祖先的距离，而不是祖先到后代的距离, 所以distance值一定是非负数；

# 数据结构

# 数据结构

# 基本属性

## 基本属性

# type

实时库的数据类型，详见枚举Enum_Rt_Type;

### type

 实时库的数据类型，详见枚举Enum_Rt_Type;

# copyGUID

拷贝GUID，创建对象时需要指定拷贝GUID；
注意：
 拷贝GUID需要在本时空的指定仓库中存在；

### copyGUID

 拷贝GUID，创建对象时需要指定拷贝GUID；

 注意：

 拷贝GUID需要在本时空的指定仓库中存在；

# modelGUID&modelVersion

对象上存有对象的模型GUID及模型版本信息，来源创建时指定的拷贝的模型；

### modelGUID&modelVersion

 对象上存有对象的模型GUID及模型版本信息，来源创建时指定的拷贝的模型；

# id

对象ID在一个OS下是唯一的，不分子系统、云、端、类型等；

### id

 对象ID在一个OS下是唯一的，不分子系统、云、端、类型等；

# reocrdID

事记录具有记录id属性；
事记录id在对象下是唯一的；

### reocrdID

 事记录具有记录id属性；

 事记录id在对象下是唯一的；

# guid

系统生成，全球唯一标识;
为保证不同云之间数据的唯一性，模型、模型拷贝、对象有全球唯一标识属性guid；

### guid

 系统生成，全球唯一标识;

 为保证不同云之间数据的唯一性，模型、模型拷贝、对象有全球唯一标识属性guid；

# name

对象名称，允许重名；
注意：
长度1-256字符，支持中文、符号、英文字母、数字，以及其他各国语言文字字母；
不能包含空格，首字符没有限制；

### name

 对象名称，允许重名；

 注意：

 长度1-256字符，支持中文、符号、英文字母、数字，以及其他各国语言文字字母；

 不能包含空格，首字符没有限制；

# displayName

显示名称。
写入规则同描述。

### displayName

 显示名称。

 写入规则同描述。

# description

对象描述，可以为空；
长度不能超过256字符，支持任意字符；

### description

 对象描述，可以为空；

 长度不能超过256字符，支持任意字符；

# parentGUID

对象的父对象GUID，默认为空串，代表没有父；
注意：
时序物的父是业务物，岗位的父是组织，人员没有父；
对象父子关系不能互为父子，也不允许出现祖先后代关系树出现闭环；
允许只删除父，不删除子，此时子上父guid不会变化；
父对象不为空串时，需要在对象的时空内存在；

### parentGUID

 对象的父对象GUID，默认为空串，代表没有父；

 注意：

 时序物的父是业务物，岗位的父是组织，人员没有父；

 对象父子关系不能互为父子，也不允许出现祖先后代关系树出现闭环；

 允许只删除父，不删除子，此时子上父guid不会变化；

 父对象不为空串时，需要在对象的时空内存在；

# parentRecordID

 业务事记录具有父记录属性；
注意：
 父记录需要在父对象的记录中存在；

### parentRecordID

 业务事记录具有父记录属性；

 注意：

 父记录需要在父对象的记录中存在；

# 时间属性

## 时间属性

# TRGUID

时间坐标系对象GUID，业务物、组织、人员具有该属性；创建时指定，不能修改；
可以为空串，代表使用系统时间坐标系；
事对象没有时间属性，其时间属性在记录上；
具体含义见上述“时间对象”的介绍；

### TRGUID

 时间坐标系对象GUID，业务物、组织、人员具有该属性；创建时指定，不能修改；

 可以为空串，代表使用系统时间坐标系；

 事对象没有时间属性，其时间属性在记录上；

 具体含义见上述“时间对象”的介绍；

# timeStamp

时间戳，物、组织、人、岗位有时间戳属性。
相对于系统时间坐标系的开始时间（1601-1-1 0:0:0utc），偏移x个100ns；

### timeStamp

 时间戳，物、组织、人、岗位有时间戳属性。

 相对于系统时间坐标系的开始时间（1601-1-1 0:0:0utc），偏移x个100ns；

# startTime

事记录的开始时间位置。
相对于系统时间坐标系的开始时间（1601-1-1 0:0:0utc），偏移x个100ns；

### startTime

 事记录的开始时间位置。

 相对于系统时间坐标系的开始时间（1601-1-1 0:0:0utc），偏移x个100ns；

# 空间属性

## 空间属性

# spaceTimeGUID

对象的时空，代表数据所处于的时空；创建对象时系统根据APP时空赋值；

### spaceTimeGUID

 对象的时空，代表数据所处于的时空；创建对象时系统根据APP时空赋值；

# 时序特有属性

## 时序特有属性

# VTQ

VTQ，是指时序物的三个重要属性：Value、TimeStamp、QualityStamp；
Value：
灵敏值，可以是数值或者字符串类型，其类型由模型的valueType（灵敏值类型）决定，实时库中不能更改值类型；
多灵敏度时序物有多个值，即灵敏成员值；
TimeStamp：
时间戳，相对于系统时间坐标系（1601-1-1 0:0:0utc）偏移的100ns的倍数；
QualityStamp：
质量戳，灵敏值质量标签，说明值的质量好坏；例如，质量戳192代表值良好；

### VTQ

 VTQ，是指时序物的三个重要属性：Value、TimeStamp、QualityStamp；

 Value：

 灵敏值，可以是数值或者字符串类型，其类型由模型的valueType（灵敏值类型）决定，实时库中不能更改值类型；

 多灵敏度时序物有多个值，即灵敏成员值；

 TimeStamp：

 时间戳，相对于系统时间坐标系（1601-1-1 0:0:0utc）偏移的100ns的倍数；

 QualityStamp：

 质量戳，灵敏值质量标签，说明值的质量好坏；例如，质量戳192代表值良好；

# freshPeriod&isFreshness

工业实时数据对于“真实性”要求往往非常高，因此，实时时序物对象具有新鲜期（freshPeriod）、是否新鲜（isFreshness）的属性，如果时序物的灵敏值/灵敏成员值不新鲜了，就表明该值是不可靠的；
时序物的灵敏值/灵敏成员值的更新时间和查询时间的时间差如果超过新鲜期，就是不新鲜的，如果在新鲜期内，就是新鲜的。
新鲜期的单位是毫秒（ms），新鲜期为0表示永远新鲜；
是否新鲜是计算字段，系统根据新鲜期、更新时间、查询时间判断后，返回true或者false；
注意，新鲜期不能为负数；

### freshPeriod&isFreshness

 工业实时数据对于“真实性”要求往往非常高，因此，实时时序物对象具有新鲜期（freshPeriod）、是否新鲜（isFreshness）的属性，如果时序物的灵敏值/灵敏成员值不新鲜了，就表明该值是不可靠的；

 时序物的灵敏值/灵敏成员值的更新时间和查询时间的时间差如果超过新鲜期，就是不新鲜的，如果在新鲜期内，就是新鲜的。

 新鲜期的单位是毫秒（ms），新鲜期为0表示永远新鲜；

 是否新鲜是计算字段，系统根据新鲜期、更新时间、查询时间判断后，返回true或者false；

 注意，新鲜期不能为负数；

# changeDeadband（单灵敏）

变化死区，和灵敏值类型一致；
实时单灵敏度时序物对象的值的变化程度只有超过（大于）变化死区，实时对象才会更新值。（新旧值变化大于变化死区的值才会更新）
如果是非数值类型，比如字符串类型、bool类型，变化死区没有意义，不起作用；
注意，变化死区不能为负数；

### changeDeadband（单灵敏）

 变化死区，和灵敏值类型一致；

 实时单灵敏度时序物对象的值的变化程度只有超过（大于）变化死区，实时对象才会更新值。（新旧值变化大于变化死区的值才会更新）

 如果是非数值类型，比如字符串类型、bool类型，变化死区没有意义，不起作用；

 注意，变化死区不能为负数；

# 列表属性

## 列表属性

# 普通索引

普通索引，用于提高过滤效率，只有支持过滤的类型的字段，才能作为普通索引；
普通索引只能加在成员上，不能加在属性上；
l 物、组织、人等非事对象支持给模型的对象添加索引，作用范围为模型的对象（不管对象是索引添加前创建的，还是索引添加后创建的），不作用于记录；
例子： 指定模型A查找对象时，可以查找到模型A的对象，即对象1~7，这时给模型A的成员1加索引，再去过滤成员1可以提高效率；
![img](系统说明.assets/wpsAC0tmp.png) 
l 时序事、业务事支持给对象添加索引，作用范围为对象的记录（不管记录是索引添加前创建的，还是索引添加后创建的）；
例子： 查找对象A的记录时，这时给成员4加索引，再去过滤成员4可以提高效率；

### 普通索引

 普通索引，用于提高过滤效率，只有支持过滤的类型的字段，才能作为普通索引；

 普通索引只能加在成员上，不能加在属性上；

 l 物、组织、人等非事对象支持给模型的对象添加索引，作用范围为模型的对象（不管对象是索引添加前创建的，还是索引添加后创建的），不作用于记录；

 例子： 指定模型A查找对象时，可以查找到模型A的对象，即对象1~7，这时给模型A的成员1加索引，再去过滤成员1可以提高效率；

 

 l 时序事、业务事支持给对象添加索引，作用范围为对象的记录（不管记录是索引添加前创建的，还是索引添加后创建的）；

 例子： 查找对象A的记录时，这时给成员4加索引，再去过滤成员4可以提高效率；

# 唯一索引（业务事对象）

唯一索引： 字段的值不能重复存在，比如工序编号等；
唯一索引可以指定多个字段，表示多个字段值不能完全相同；
唯一索引支持类型：字符串型、日期时间类型、整型、decimal、浮点型。注：由于浮点数存在精度问题，使用时要谨慎，如果使用浮点型，可能出现随机误差。
唯一索引字段只能是成员，不能是属性；
唯一索引支持加在业务事对象上，作用范围为记录，即记录的多个成员值不能完全相同。
l 如果作用范围内，已经有唯一索引字段重复的值，则添加唯一索引失败；
唯一索引的成员值可以为null，null和null不算重复；
l 唯一索引的过滤加强作用
唯一索引可以提高过滤效率。唯一索引的过滤效率与索引的顺序有关，遵循最左前缀匹配原则。
比如唯一索引的顺序是 “a,b,c”,那么：
能命中索引的过滤条件： a==? 、a==?&&b==?、a==?&&b==?&&c==?
无法命中索引的过滤条件：b==?、b==?&&c==？（由于缺少最左侧字段a）
建议用户将高频过滤的字段放前面，比如经常单独过滤a，则a应该放在索引的左侧；将过滤性强的字段放前面（比如“学号”比“性别”过滤性强），过滤性强的列放前面，能更快缩小查询范围，减少索引扫描量。
总结：唯一索引的字段顺序不影响“组合值唯一”的约束逻辑，但是影响索引能否被过滤利用，需将高频查询字段和过滤性强的字段放在左侧。

### 唯一索引（业务事对象）

 唯一索引： 字段的值不能重复存在，比如工序编号等；

 唯一索引可以指定多个字段，表示多个字段值不能完全相同；

 唯一索引支持类型：字符串型、日期时间类型、整型、decimal、浮点型。注：由于浮点数存在精度问题，使用时要谨慎，如果使用浮点型，可能出现随机误差。

 唯一索引字段只能是成员，不能是属性；

 唯一索引支持加在业务事对象上，作用范围为记录，即记录的多个成员值不能完全相同。

 l 如果作用范围内，已经有唯一索引字段重复的值，则添加唯一索引失败；

 唯一索引的成员值可以为null，null和null不算重复；

 l 唯一索引的过滤加强作用

 唯一索引可以提高过滤效率。唯一索引的过滤效率与索引的顺序有关，遵循最左前缀匹配原则。

 比如唯一索引的顺序是 “a,b,c”,那么：

 能命中索引的过滤条件： a==? 、a==?&&b==?、a==?&&b==?&&c==?

 无法命中索引的过滤条件：b==?、b==?&&c==？（由于缺少最左侧字段a）

 建议用户将高频过滤的字段放前面，比如经常单独过滤a，则a应该放在索引的左侧；将过滤性强的字段放前面（比如“学号”比“性别”过滤性强），过滤性强的列放前面，能更快缩小查询范围，减少索引扫描量。

 总结：唯一索引的字段顺序不影响“组合值唯一”的约束逻辑，但是影响索引能否被过滤利用，需将高频查询字段和过滤性强的字段放在左侧。

# 外键（业务事对象）

时空库提供单库外键功能，不提供跨库外键功能；
时空库提供两个业务事对象之间的外键功能；
ü 外键定义：
一个对象的成员mf可以被设为外键，外键需要引用另外一个对象的成员mp；
此场景中，成员mf称为外键，其所在对象可称为从对象，成员mp称为主键，其所在的对象可称为主对象；
ü 增加外键的要求：
 外键与主键的成员类型必须严格一致，否则增加外键失败；
 主键必须首先为主对象的主键或者唯一索引，才能被外键引用；（尚未提供主键功能，因此目前要求外键的主键必须为唯一索引）
 已经存在记录的对象的成员被设为外键时，必须保证所有的外键值在主对象的记录中存在对应的主键值，否则增加外键失败；
ü 外键的约束作用：
1、 操作外键时
1创建限制
从对象在插入记录时，若外键的值在主对象中不存在主键为此值的记录，则记录插入失败；
2更新限制
 更新外键时，若外键的值在主对象中不存在主键为此值的记录，则记录更新失败；
2、 操作主键时
1更新限制
-- 严格模式：
 更新主键时，若从对象下存在引用记录，则不能更新；
2删除限制
-- 严格模式：
主对象记录删除时，若从对象下存在引用记录，则不能删除；

### 外键（业务事对象）

 时空库提供单库外键功能，不提供跨库外键功能；

 时空库提供两个业务事对象之间的外键功能；

 ü 外键定义：

 一个对象的成员mf可以被设为外键，外键需要引用另外一个对象的成员mp；

 此场景中，成员mf称为外键，其所在对象可称为从对象，成员mp称为主键，其所在的对象可称为主对象；

 ü 增加外键的要求：

 外键与主键的成员类型必须严格一致，否则增加外键失败；

 主键必须首先为主对象的主键或者唯一索引，才能被外键引用；（尚未提供主键功能，因此目前要求外键的主键必须为唯一索引）

 已经存在记录的对象的成员被设为外键时，必须保证所有的外键值在主对象的记录中存在对应的主键值，否则增加外键失败；

 ü 外键的约束作用：

 1、 操作外键时

 1创建限制

 从对象在插入记录时，若外键的值在主对象中不存在主键为此值的记录，则记录插入失败；

 2更新限制

 更新外键时，若外键的值在主对象中不存在主键为此值的记录，则记录更新失败；

 2、 操作主键时

 1更新限制

 -- 严格模式：

 更新主键时，若从对象下存在引用记录，则不能更新；

 2删除限制

 -- 严格模式：

 主对象记录删除时，若从对象下存在引用记录，则不能删除；