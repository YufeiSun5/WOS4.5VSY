# 方案设计

采用一个新的右侧内容页面精灵图承载演示，不覆盖 `PalimpsestContent_82`。

核心方案：

- 页面名：`PalimpsestExpandTable_82_Demo`
- 视觉：浅色管理后台，紧凑搜索区、统计卡片、主表格。
- 表格：自绘展开行或同层展开结构，避免 Element UI fixed-right 克隆表导致高度不同步。
- 数据：内置假数据，保留后续替换为后端返回数组的入口。
