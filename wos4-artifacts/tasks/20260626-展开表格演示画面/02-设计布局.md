# 设计布局

## 画面比例

目标是右侧内容画面，按 82% 内容区思路设计。画面本身按 1920x1080 右侧区域承载，内部不再包含左侧菜单。

## 区域切分

- 顶部：标题、说明、搜索输入、状态筛选、主操作按钮。
- 指标区：设备总数、在线率、待维护、异常数。
- 主表格区：设备台账表格，支持展开明细。
- 展开区：使用同一主表格层内的详情行，不使用 fixed 右列。

## 参考表格问题规避

参考表格的错位来自 Element UI 的 fixed-right 克隆表格与主表格展开行高度不同步。新画面不使用固定操作列，展开按钮和操作入口都在主表格同一布局流内。
