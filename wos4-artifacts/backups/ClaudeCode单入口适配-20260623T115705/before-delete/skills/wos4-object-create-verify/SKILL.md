---
description: "Claude Code adapter for `.ai/skills/wos4-object-create-verify`. Use to create and verify a WOS4 App/FU object from a deployed running repository copy."
---

# wos4-object-create-verify

这是 Claude Code 兼容 wrapper。真实 skill 在 `.ai/skills/wos4-object-create-verify/SKILL.md`。

执行前必须：

1. 读取 `AGENTS.md`。
2. 完整读取 `.ai/skills/wos4-object-create-verify/SKILL.md`。
3. 若真实 skill 引用相对路径，按 `.ai/skills/wos4-object-create-verify/` 解析。
4. 按真实 skill 执行；不要在本 wrapper 中推断或替代流程。
