---
description: "Claude Code adapter for `.ai/skills/wos4-style-config`. Use for WOS4 styleConfig and detailConfig styling with persistence validation."
---

# wos4-style-config

这是 Claude Code 兼容 wrapper。真实 skill 在 `.ai/skills/wos4-style-config/SKILL.md`。

执行前必须：

1. 读取 `AGENTS.md`。
2. 完整读取 `.ai/skills/wos4-style-config/SKILL.md`。
3. 若真实 skill 引用相对路径，按 `.ai/skills/wos4-style-config/` 解析。
4. 按真实 skill 执行；不要在本 wrapper 中推断或替代流程。
