---
description: "Claude Code adapter for `.ai/skills/wechat-send-file-transfer`. Use when sending a local file to WeChat File Transfer Assistant through the logged-in desktop client."
---

# wechat-send-file-transfer

这是 Claude Code 兼容 wrapper。真实 skill 在 `.ai/skills/wechat-send-file-transfer/SKILL.md`。

执行前必须：

1. 读取 `AGENTS.md`。
2. 完整读取 `.ai/skills/wechat-send-file-transfer/SKILL.md`。
3. 若真实 skill 引用相对路径，按 `.ai/skills/wechat-send-file-transfer/` 解析。
4. 按真实 skill 执行；不要在本 wrapper 中推断或替代流程。
